# v68 UI ratio helper fix

- 主導権入力監査の表示時に `fmtRatio is not defined` で落ちる不具合を修正。
- `fmtRatio()` を監査UI共通の数値フォーマッタとして追加。
- 主導権入力監査だけでなく、既存の展開枝監査・採用終端監査で同ヘルパーを使う箇所も安全に表示できる。
- 予想ロジック、確率、買い目採用、保存形式、Railway側は変更していない。
