
import{parseRaceIndexHtml}from"../../boat/parser/raceindex-parser.mjs";import{validDate,jsonResponse}from"../../boat/parser/utils.mjs";
export default async req=>{const u=new URL(req.url),date=u.searchParams.get("date")||"",jcd=u.searchParams.get("jcd")||"",lead=Number(u.searchParams.get("lead")||5);if(!validDate(date)||!/^\d{2}$/.test(jcd))return jsonResponse(400,{ok:false,error:"パラメータ不正"});const source=`https://www.boatrace.jp/owpc/pc/race/raceindex?hd=${date}&jcd=${jcd}`;try{const r=await fetchOfficial(source);if(!r.ok)return jsonResponse(502,{ok:false,error:`HTTP ${r.status}`});const parsed=parseRaceIndexHtml(await r.text(),lead,{date,jcd});return jsonResponse(200,{ok:parsed.ok,date,jcd,...parsed,source,checkedAt:new Date().toISOString()})}catch(e){return jsonResponse(500,{ok:false,error:e.message,source})}};
async function fetchOfficial(url){return fetch(url,{headers:{"user-agent":"Mozilla/5.0 (compatible; ChariNekoDev/0.6; personal-use)","accept-language":"ja"}})}
