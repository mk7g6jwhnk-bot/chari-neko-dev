import assert from"node:assert/strict";import{summarizeResearchLearning}from"../public/prediction-store.mjs";const rows=[],venues=["立川","平塚","前橋","富山"];for(let i=0;i<64;i++){const confirmed=(i%10)!==9;rows.push({predictionSnapshotId:`P${i}`,learningMode:"NORMAL",checkedAt:`2026-08-${String(1+Math.floor(i/6)).padStart(2,"0")}T00:00:00Z`,venueName:venues[i%4],conditionEvidence:[{evidenceKey:`E:${i}`,conditionId:`MAKURI_REACH_${i+1}`,stage:"FIRST",kind:"natural",predictedProbability:.68,status:confirmed?"CONFIRMED":"REFUTED",source:"official_winning_method",autoResolved:true}],verificationStatus:"PURCHASE_SELECTION_MISS",exactTerminalGenerated:true,firstPlaceFamilyGenerated:true,firstSecondPairGenerated:true})}const storage={getItem:k=>k==="chari-neko:keirin-research-learning:v1"?JSON.stringify(rows):null,setItem(){}};const cal=summarizeResearchLearning(storage).conditionCalibration,g=cal.groups[0],pkg=g.promotionPackage;assert.equal(g.independentAudit.status,"INDEPENDENT_AUDIT_PASS");assert.equal(pkg.status,"PROMOTION_PACKAGE_READY");assert.equal(pkg.rolloutPolicy.mode,"SHADOW_ONLY");assert.equal(pkg.rolloutPolicy.productionWriteAllowed,false);assert.equal(pkg.rolloutPolicy.requiresManualApproval,true);assert.ok(pkg.rollbackPolicy.reviewAfterSamples>=20);assert.equal(cal.promotionPackageReadyCount,1);console.log("PASS promotion package generation + no production write");