import assert from"node:assert/strict";import{attachResult,loadResearchLearningRecords,researchEvidenceQueue,saveSnapshot,updateResearchConditionEvidence}from"../public/prediction-store.mjs";
const mem=new Map(),storage={getItem:k=>mem.get(k)||null,setItem:(k,v)=>mem.set(k,String(v))};
const s={predictionSnapshotId:"E1",predictionVersion:"X",createdAt:"2026-08-10T00:00:00Z",targetRace:{date:"20260810",venueName:"立川",venueCode:"28",raceNo:1},participants:[],abilitiesUsed:[],predictionOutput:{},branches:[],betSelections:[],terminalLedger:[{order:[1,2,3],probability:.2,purchaseStatus:"購入不採用",nodeSummary:{FIRST:{conditionalProbability:.3,newConditionCount:1,extraConditionCount:0,conditions:[{id:"F1",label:"1が主導権",kind:"natural",probability:.7,critical:true}]},SECOND:{conditionalProbability:.4,newConditionCount:1,extraConditionCount:0,conditions:[{id:"S1",label:"2が追走",kind:"natural",probability:.8,critical:true}]},THIRD:{conditionalProbability:.5,newConditionCount:1,extraConditionCount:1,conditions:[{id:"T1",label:"3が3着残り",kind:"extra",probability:.5,critical:true}]}}}],oddsSnapshot:null,result:null};
saveSnapshot(storage,s);attachResult(storage,"E1",{status:"confirmed",finishOrder:[1,2,3]});
let q=researchEvidenceQueue(storage);assert.equal(q.length,3);assert.ok(q.every(x=>x.status==="EVIDENCE_PENDING"));
updateResearchConditionEvidence(storage,{snapshotId:"E1",evidenceKey:q[0].evidenceKey,status:"CONFIRMED",source:"manual_review"});updateResearchConditionEvidence(storage,{snapshotId:"E1",evidenceKey:q[1].evidenceKey,status:"REFUTED",source:"manual_review"});updateResearchConditionEvidence(storage,{snapshotId:"E1",evidenceKey:q[2].evidenceKey,status:"UNKNOWN",source:"manual_review"});
const r=loadResearchLearningRecords(storage)[0];assert.equal(r.evidenceSummary.pending,0);assert.equal(r.evidenceSummary.confirmed,1);assert.equal(r.evidenceSummary.refuted,1);assert.equal(r.evidenceSummary.unknown,1);assert.equal(r.evidenceSummary.reviewComplete,true);assert.equal(r.evidenceSummary.decisiveEvidenceComplete,false);assert.equal(r.nodeCauseLearningEligible,false);console.log("PASS condition evidence review queue");