import assert from'node:assert/strict';import{audit2Tan}from'../research/2tan-shadow-strategy.mjs';
const terminal=(o,extra={})=>({order:o,globalRank:1,pairRank:1,purchaseRejectCode:'ADOPTED',...extra});
const input=(raceKey,finish,standard,payouts,extra={})=>({raceKey,abnormalAuditKnown:true,prediction:{predictionPayload:{prediction:{performanceSchemaVersion:'PURCHASE_PERFORMANCE_V2',purchaseEligibility:{canPurchase:true},standardPurchasePlan:standard,purchase:{audit:{terminalLifecycleAudit:{rows:[terminal([1,2,3]),terminal([1,2,4],{purchaseRejectCode:'THIRD_VARIANT_BOUNDARY'})]}}}}}},result:{officialResult:{status:'confirmed',finishOrder:finish,payout:payouts.trifecta,payouts:{exacta:[{combination:finish.slice(0,2),payout:payouts.exacta}]}}},...extra});
const r=audit2Tan([input('20260901-01-1',[1,2,4],[terminal([1,2,3],{betClass:'MAIN'})],{trifecta:900,exacta:500}),input('20260901-01-2',[1,2,3],[terminal([1,2,3],{betClass:'MAIN'})],{trifecta:1200,exacta:400}),input('20260901-01-3',[1,2,3],[terminal([1,2,3],{betClass:'MAIN'})],{trifecta:1200,exacta:400},{abnormal:true})]);
assert.equal(r.formalCohort,2);assert.equal(r.headToHeadDescriptive.pairOnly,1);assert.equal(r.headToHeadDescriptive.both,1);assert.equal(r.descriptivePeriods.cumulative.exacta.hitRate,1);assert.equal(r.descriptivePeriods.cumulative.exacta.roi,4.5);assert.equal(r.descriptivePeriods.cumulative.trifecta.roi,6);assert.equal(r.thirdDropsDescriptive[0].minimumFixedThirdSpread,2);assert.deepEqual(r.excluded.abnormal,['20260901-01-3']);assert.equal(r.safety.productionWrite,0);console.log('PASS 2tan read-only shadow strategy');
