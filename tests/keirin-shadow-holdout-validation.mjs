import assert from"node:assert/strict";import{summarizeResearchLearning}from"../public/prediction-store.mjs";
const rows=[];for(let i=0;i<40;i++){const confirmed=(i%10)!==9;rows.push({predictionSnapshotId:`H${i}`,learningMode:"NORMAL",checkedAt:`2026-08-${String(1+Math.floor(i/4)).padStart(2,"0")}T${String((i%4)*3).padStart(2,"0")}:00:00Z`,conditionEvidence:[{evidenceKey:`F:${i}`,conditionId:`MAKURI_REACH_${i+1}`,stage:"FIRST",kind:"natural",predictedProbability:.68,status:confirmed?"CONFIRMED":"REFUTED"}],verificationStatus:"PURCHASE_SELECTION_MISS",exactTerminalGenerated:true,firstPlaceFamilyGenerated:true,firstSecondPairGenerated:true})}
const storage={getItem:k=>k==="chari-neko:keirin-research-learning:v1"?JSON.stringify(rows):null,setItem(){}};const cal=summarizeResearchLearning(storage).conditionCalibration,g=cal.groups[0],h=g.holdoutValidation;
assert.equal(g.shadowProposal.status,"READY_FOR_RESEARCH_REVIEW");assert.equal(h.status,"HOLDOUT_PASS");assert.equal(h.directionConsistent,true);assert.ok(h.brierImprovement>0);assert.ok(h.logLossImprovement>0);assert.equal(cal.holdoutPassedCount,1);assert.equal(cal.productionApplyEnabled,false);console.log("PASS chronological holdout validation gate");