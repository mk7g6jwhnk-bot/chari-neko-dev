import assert from"node:assert/strict";import{summarizeResearchLearning}from"../public/prediction-store.mjs";
const rows=[];for(let i=0;i<40;i++){const confirmed=i<28?((i%4)!==3):true;rows.push({predictionSnapshotId:`L${i}`,learningMode:"NORMAL",checkedAt:`2026-08-${String(1+Math.floor(i/4)).padStart(2,"0")}T00:00:00Z`,venueName:["立川","平塚","前橋","富山"][i%4],conditionEvidence:[{evidenceKey:`E${i}`,conditionId:`MAKURI_REACH_${i+1}`,stage:"FIRST",kind:"natural",predictedProbability:.68,status:confirmed?"CONFIRMED":"REFUTED"}],verificationStatus:"PURCHASE_SELECTION_MISS",exactTerminalGenerated:true,firstPlaceFamilyGenerated:true,firstSecondPairGenerated:true})}
const storage={getItem:k=>k==="chari-neko:keirin-research-learning:v1"?JSON.stringify(rows):null,setItem(){}};const g=summarizeResearchLearning(storage).conditionCalibration.groups[0];
assert.equal(g.descriptiveShadowProposal.status,"READY_FOR_RESEARCH_REVIEW");assert.equal(g.trainCandidate.sampleCount,28);assert.notEqual(g.trainCandidate.proposal.status,"READY_FOR_RESEARCH_REVIEW");assert.equal(g.holdoutValidation.status,"TRAIN_NOT_READY");assert.equal(g.holdoutValidation.selectionLeakagePrevented,true);assert.equal(g.holdoutValidation.holdoutSealed,true);console.log("PASS holdout selection leakage prevented by train-only candidate gate");