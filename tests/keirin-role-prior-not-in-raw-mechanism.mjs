import assert from"node:assert/strict";import fs from"node:fs";const s=fs.readFileSync(new URL("../keirin/sports/keirin-scoring.mjs",import.meta.url),"utf8");const b=s.slice(s.indexOf("const firstMechanisms"),s.indexOf("const abilityPlacement"));assert.equal(b.includes('item("rolePrior"'),false);assert.ok(s.includes("applyRoleContext"));assert.ok(s.includes("RAW_ABILITY_FIRST_ROLE_CONTEXT_SECOND"));console.log("PASS role prior removed from raw mechanism scoring");