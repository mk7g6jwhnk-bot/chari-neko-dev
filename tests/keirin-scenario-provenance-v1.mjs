import assert from"node:assert/strict";import{attachScenarioProvenanceId,buildScenarioProvenance,SCENARIO_PROVENANCE_SCHEMA_VERSION}from"../keirin/engine/scenario-provenance.mjs";
const lines=[{id:"L1",members:[{number:1,linePosition:0},{number:2,linePosition:1}]},{id:"L2",members:[{number:4,linePosition:0},{number:5,linePosition:1}]}],branches=[{id:"LEAD-L1",branchType:"LEADER_HOLD",primaryLineId:"L1"},{id:"MAKURI-L2",branchType:"MAKURI_SUCCESS",primaryLineId:"L2"}],terminals=[{order:[1,2,3],branchId:"LEAD-L1"},{order:[1,2,4],branchId:"LEAD-L1"},{order:[4,5,1],branchId:"MAKURI-L2"}];
const before=structuredClone({terminals,branches,lines}),out=buildScenarioProvenance({terminals,branches,lines});assert.deepEqual({terminals,branches,lines},before,"metadata builder must not mutate prediction inputs");assert.equal(out.scenarioProvenanceSchemaVersion,SCENARIO_PROVENANCE_SCHEMA_VERSION);assert.equal(out.audit.scenarioCount,2);assert.equal(out.audit.mappedTerminalCount,3);assert.equal(out.audit.thirdPositionUsed,false);assert.deepEqual(out.audit.resultFieldsUsed,[]);const mapped=terminals.map(row=>attachScenarioProvenanceId(row,out.terminalScenarioIds));assert.equal(mapped[0].scenarioProvenanceId,mapped[1].scenarioProvenanceId);assert.notEqual(mapped[0].scenarioProvenanceId,mapped[2].scenarioProvenanceId);assert.ok(out.scenarioProvenances[mapped[0].scenarioProvenanceId]);console.log("scenario provenance v1: PASS");
