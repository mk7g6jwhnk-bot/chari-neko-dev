import assert from "node:assert/strict";
import fs from "node:fs";
const src=fs.readFileSync(new URL("../public/app.mjs",import.meta.url),"utf8");
assert.ok(src.includes('class="buyExplanation"'), "individual buy explanation details missing");
assert.ok(src.includes('<small>理由を見る</small>'), "per-buy explanation affordance missing");
assert.ok(src.includes("friendlyPurchaseReason(b,explanation=null,snapshot=null)"), "user-facing purchase reason missing");
const css=fs.readFileSync(new URL("../public/styles.css",import.meta.url),"utf8");
assert.ok(css.includes(".buyExplanation>summary"), "buy explanation summary style missing");
const version=JSON.parse(fs.readFileSync(new URL("../public/version.json",import.meta.url),"utf8"));
assert.equal(version.label,"v245-no-blanket-skip");
console.log("PASS v240 all buy explanation UI");
