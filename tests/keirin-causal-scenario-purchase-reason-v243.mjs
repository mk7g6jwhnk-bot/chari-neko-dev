import assert from "node:assert/strict";
import fs from "node:fs";
const src=fs.readFileSync(new URL("../public/app.mjs",import.meta.url),"utf8");
assert.ok(src.includes("この買い目になるシナリオ"));
assert.ok(src.includes("scenarioIntegrity"));
assert.ok(src.includes("予測枝"));
assert.ok(src.includes("詳細な成立根拠を見る"));
assert.ok(!src.includes('causalReasons.map(x=>`<li>'));
const explanation=fs.readFileSync(new URL("../modules/keirin/engine/prediction-explanation.mjs",import.meta.url),"utf8");
assert.ok(explanation.includes("scenarioIntegrity"));
assert.ok(explanation.includes("BRANCH_TIMELINE_WITH_NODE_TRACE"));
console.log("PASS v244 causal scenario linkage");
