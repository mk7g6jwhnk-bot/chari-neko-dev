import assert from"node:assert/strict";import{classifyScenarioIdentity,evaluateScenarioIdentity,groupScenarioIdentities,SCENARIO_IDENTITY_VERSION}from"../research/scenario-identity-v2.mjs";
const rows=[{order:[1,2,3],probability:.3,dominantBranchId:"LEAD-L1"},{order:[1,2,4],probability:.2,dominantBranchId:"LEAD-L1"},{order:[2,1,3],probability:.15,dominantBranchId:"BANTE-L1"},{order:[4,5,1],probability:.1,dominantBranchId:"MAKURI-L2"}];
const record={raceKey:"X",sealed:{predictionSealedAt:"2026-09-09",participants:[{number:1,lineId:"L1",linePosition:0},{number:2,lineId:"L1",linePosition:1},{number:3,lineId:"L2",linePosition:0},{number:4,lineId:"L2",linePosition:0},{number:5,lineId:"L2",linePosition:1}],researchPrediction:{branches:[{id:"LEAD-L1",branchType:"LEADER_HOLD",primaryLineId:"L1"},{id:"BANTE-L1",branchType:"BANTE_SASHI",primaryLineId:"L1"},{id:"MAKURI-L2",branchType:"MAKURI_SUCCESS",primaryLineId:"L2"}],terminals:rows}},result:{result:{status:"confirmed",finishOrder:[1,2,4],payout:9999}}};
const a=classifyScenarioIdentity(record,rows[0]),b=classifyScenarioIdentity(record,rows[1]);assert.equal(a.id,b.id);assert.equal(a.thirdPositionUsed,false);assert.deepEqual(a.resultFieldsUsed,[]);assert.equal(groupScenarioIdentities(record).length,3);
const leaked=structuredClone(record);leaked.result.result.finishOrder=[7,6,5];leaked.result.result.payout=1;assert.deepEqual(groupScenarioIdentities(leaked),groupScenarioIdentities(record));
const report=evaluateScenarioIdentity([record]);assert.equal(report.version,SCENARIO_IDENTITY_VERSION);assert.equal(report.resultEvaluation.top1,1);assert.equal(report.productionWriteAllowed,false);console.log("PASS scenario identity v2");
