import assert from"node:assert/strict";import{summarizeResearchLearning}from"../public/prediction-store.mjs";
const rows=[];for(let i=0;i<40;i++){const confirmed=(i%10)!==9;rows.push({predictionSnapshotId:`S${i}`,learningMode:"NORMAL",checkedAt:`2026-08-${String(1+Math.floor(i/4)).padStart(2,"0")}T${String((i%4)*3).padStart(2,"0")}:00:00Z`,conditionEvidence:[{evidenceKey:`F:${i}`,conditionId:`MAKURI_REACH_${i+1}`,stage:"FIRST",kind:"natural",predictedProbability:.68,status:confirmed?"CONFIRMED":"REFUTED",source:"official_winning_method",autoResolved:true}],verificationStatus:"PURCHASE_SELECTION_MISS",exactTerminalGenerated:true,firstPlaceFamilyGenerated:true,firstSecondPairGenerated:true})}
const storage={getItem:k=>k==="chari-neko:keirin-research-learning:v1"?JSON.stringify(rows):null,setItem(){}};const s=summarizeResearchLearning(storage),cal=s.conditionCalibration,g=cal.groups[0];
assert.equal(g.sampleCount,40);assert.equal(g.family,"MAKURI_REACH_N");assert.equal(g.temporalStability.status,"STABLE_DIRECTION");assert.equal(g.shadowProposal.status,"READY_FOR_RESEARCH_REVIEW");assert.ok(g.shadowProposal.suggestedProbability>.68);assert.ok(g.shadowProposal.suggestedProbability<g.observedRate);assert.equal(cal.shadowProposalCount,1);assert.equal(cal.productionApplyEnabled,false);assert.equal(cal.autoPromotionEnabled,false);console.log("PASS shadow calibration proposal + shrinkage + stability gate");