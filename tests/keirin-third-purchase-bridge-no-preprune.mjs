import assert from"node:assert/strict";import fs from"node:fs";const s=fs.readFileSync(new URL("../keirin/engine/chat-spec-v1-policy.mjs",import.meta.url),"utf8");assert.ok(s.includes(".filter(x=>x.firstFamilyNumber===family.first && x.pairNaturalPositionEligible)"));assert.ok(s.includes("evaluateThirdPurchaseBridge(group.rows,{allowNearTieBreadth:!isAdditionalSecondNearPeer"));assert.ok(s.includes("THIRD_PURCHASE_BRIDGE_NOT_SELECTED"));console.log("PASS no third pre-purchase gate");