import assert from"node:assert/strict";import{extractOfficialResult}from"../src/keirin-result.mjs";
const expected={date:"20260808",venueCode:"55",venueName:"和歌山",raceNo:1,verifiedIdentity:{date:"20260808",venueName:"和歌山",raceNo:1}};
const finished={JSJ012:{txtEventDate:"2026/08/08",joCd:"55",joName:"和歌山",selRaceNo:1,tyakujyunDispFlg:true,tyakujyunItemSubData:[{tyaku:"1",syaban:"3"},{tyaku:"2",syaban:"5"},{tyaku:"3",syaban:"1"}],haraiGakuDispFlg:true,haraiGakuSubData:{RT3HaraiGakuDispItemSubData:[{kumiBan:"3-5-1",haraiGaku:"12,340"}]}}};
const result=extractOfficialResult(finished,expected,new Date("2026-08-08T06:00:00Z"));assert.equal(result.status,"finished");assert.deepEqual(result.finishOrder,[3,5,1]);assert.equal(result.payouts.trifecta[0].combination,"3-5-1");assert.equal(result.payouts.trifecta[0].payout,12340);assert.equal(result.identityPassed,true);
const unfinished=extractOfficialResult({JSJ012:{txtEventDate:"20260808",joCd:"55",joName:"和歌山",selRaceNo:2,status:"発売中"}},{...expected,raceNo:2,verifiedIdentity:{...expected.verifiedIdentity,raceNo:2}});assert.equal(unfinished.status,"not_finished");assert.deepEqual(unfinished.finishOrder,[]);
for(const mismatch of[{date:"20260807"},{venueCode:"56"},{raceNo:9}]){const changed={...finished.JSJ012,...(mismatch.date?{txtEventDate:mismatch.date}:{}),...(mismatch.venueCode?{joCd:mismatch.venueCode}:{}),...(mismatch.raceNo?{selRaceNo:mismatch.raceNo}:{})};assert.equal(extractOfficialResult({JSJ012:changed},expected).status,"identity_mismatch")}
assert.equal(extractOfficialResult({JSJ012:{...finished.JSJ012,status:"中止",tyakujyunItemSubData:[],haraiGakuSubData:{}}},expected).status,"cancelled");assert.equal(extractOfficialResult({JSJ012:{...finished.JSJ012,status:"返還",tyakujyunItemSubData:[],haraiGakuSubData:{}}},expected).status,"refund");
const noInference=extractOfficialResult({JSJ012:{txtEventDate:"20260808",joCd:"55",joName:"和歌山",selRaceNo:1,odds:{"3-5-1":12.3},popularity:[3,5,1],prediction:[3,5,1]}},expected);assert.equal(noInference.status,"not_finished");assert.deepEqual(noInference.finishOrder,[]);console.log("Keirin official result safety tests passed.");
