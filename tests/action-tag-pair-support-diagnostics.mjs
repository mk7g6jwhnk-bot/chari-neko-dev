import assert from'node:assert/strict';import{actionTagDiagnostics,nextCollectionStage}from'../research/action-tag-pair-support-diagnostics.mjs';
const tag=(type,value,status='POSSIBLE',lane='AUTO_CANDIDATE')=>({riderId:'R1',stateType:type,stateValue:value,verificationStatus:status,collectionLane:lane,evidenceType:lane==='AUTO_CANDIDATE'?'MULTI_SOURCE_STRONG_PROXY':'VALIDATED_MANUAL_OBSERVATION',evidenceSource:'e',confidence:.7,observationPhase:'POST_RESULT_OBSERVATION',context:{conditionalCells:['BANTE']}}),race={raceKey:'R',record:{participants:[{registration:'R1'}]},tags:[tag('LINE_TRACKING','SUCCESS')]};
const store={async *events(kind){if(kind==='races')yield race;},async *raceReviews(){yield{tags:[tag('BANTE_RESPONSE','UNKNOWN','UNKNOWN','MANUAL_REVIEW'),tag('LINE_TRACKING','SUCCESS','CONFIRMED','MANUAL_REVIEW')],judgments:[{disagreement:true}]};}};const x=await actionTagDiagnostics(store);assert.equal(x.races,1);assert.equal(x.strongProxy,1);assert.equal(x.unknown,1);assert.equal(x.manualCompletedRaces,1);assert.equal(x.conflicts,1);assert.equal(x.conditionalCells['LINE_TRACKING:SUCCESS:BANTE'],1);assert.equal(x.pairSupport.tracking.status,'PARTIAL');assert.equal(x.gates.race300.complete,false);assert.deepEqual(nextCollectionStage(20),{current:20,target:50,remaining:30,batchLimit:10,readyForNextStage:false});assert.equal(nextCollectionStage(50).target,100);console.log('PASS staged collection, conflicts, pair support diagnostics, rider summary, cells, gates');
