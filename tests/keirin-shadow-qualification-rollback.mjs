import assert from"node:assert/strict";import{evaluateShadowQualification}from"../public/prediction-store.mjs";
const make=(id,imp)=>({comparisonId:`C${id}`,createdAt:`2026-08-${String(1+Math.floor(id/4)).padStart(2,"0")}T00:00:00Z`,status:"RESULT_ATTACHED",methodologyEpoch:"PROMOTION-METHOD-2026-08-V2-SEALED-ISOLATED",shadowEvaluationEpoch:"SHADOW-EVAL-ISOLATED-NORMALIZED-V1",adjustments:[{packageKey:"PKG"}],evaluationIntegrity:"ISOLATED_NORMALIZED",packageOutcomes:{PKG:{winner:imp>0?"SHADOW":imp<0?"CURRENT":"TIE",logLossImprovement:imp,qualificationEligible:true}}});
const good=Array.from({length:24},(_,i)=>make(i,i%5===0?.01:.08));let q=evaluateShadowQualification(good).packages[0];assert.equal(q.status,"SHADOW_VALIDATED");assert.ok(q.winShare>=.55);assert.equal(q.temporalStable,true);assert.equal(q.productionPromotionAllowed,false);
const bad=Array.from({length:24},(_,i)=>make(i,i<12?.08:-.09));q=evaluateShadowQualification(bad).packages[0];assert.equal(q.status,"ROLLBACK_RECOMMENDED");
const small=Array.from({length:10},(_,i)=>make(i,.05));q=evaluateShadowQualification(small).packages[0];assert.equal(q.status,"SAMPLE_BUILDING");
const legacy=[{comparisonId:"L",status:"RESULT_ATTACHED",methodologyEpoch:"PROMOTION-METHOD-2026-08-V2-SEALED-ISOLATED",shadowEvaluationEpoch:"SHADOW-EVAL-ISOLATED-NORMALIZED-V1",adjustments:[{packageKey:"OLD"}],outcome:{winner:"SHADOW",logLossImprovement:1}}];const l=evaluateShadowQualification(legacy);assert.equal(l.packages.length,0);assert.equal(l.excludedLegacy,1);
console.log("PASS isolated shadow qualification + legacy exclusion");