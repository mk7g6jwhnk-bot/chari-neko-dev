# v56 一次選別・締切時刻優先取得

- 一次選別候補は開催情報に含まれる公式締切時刻を最優先の正本として選ぶ。
- 最初の6Rで失敗しても候補探索を最大24Rまで続け、55秒以内に最低3Rの成功を目指す。
- 締切時刻付き候補が3R未満のときだけ active-races を補助探索として使用する。
- 一括previewの失敗理由をレース単位で保持し、一次選別画面に「未公開・未発売 / データ準備中 / タイムアウト」等で表示する。
- 一次選別不成立時の直接深掘りは3候補固定ではなく、最大6候補から3R成功するまで補充する。
- 一次選別進捗キーをv5へ更新し、旧v55の失敗状態を引き継がない。
- 予想エンジン・買い目生成ロジックは変更しない。
