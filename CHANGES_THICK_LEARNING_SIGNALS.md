# v138 厚め失敗・学習シグナル集計

- v137 の厚め失敗段階を件数だけでなく評価母数に対する率・厚め外し内の構成比で集計。
- 少数事例からの後付け学習を防ぐため、研究候補判定に最低母数・最低失敗数を導入。
- デフォルト暫定値は厚め評価30R以上、厚め外し10R以上。閾値は研究設定であり本番値ではない。
- 頭/2着/3着の失敗構成比が暫定35%以上の場合のみ `RESEARCH_REVIEW_CANDIDATE`。
- 候補化されても本番ロジック、自動昇格、確率、買い目、資金配分は変更しない。
- `productionWriteAllowed=false` を維持。
