diff -ruN /mnt/data/ui_v10_orig/chari-neko-dev-core-v9/public/app.mjs /mnt/data/ui_v10_work/chari-neko-dev-core-v9/public/app.mjs
--- /mnt/data/ui_v10_orig/chari-neko-dev-core-v9/public/app.mjs	2026-08-08 11:32:47.000000000 +0000
+++ /mnt/data/ui_v10_work/chari-neko-dev-core-v9/public/app.mjs	2026-08-08 14:18:46.639675455 +0000
@@ -19,11 +19,14 @@
 function openRaces(meeting){state.meeting=meeting;$("venueTitle").textContent=meeting.venueName;$("raceDateLabel").textContent=state.date;const races=Array.from({length:12},(_,i)=>raceFrom(meeting,i+1));$("raceCount").textContent="12R";$("raceList").innerHTML=races.map((r,i)=>`<button class="raceCard" data-race="${i}"><div class="raceTop"><h2>${r.raceNo}R</h2><span class="status">${raceStatus(r).label}</span></div><p>発走・締切 ${r.scheduledStart||"公式情報で確認"}</p><span class="pill">詳細を見る</span></button>`).join("");$("raceList").querySelectorAll("[data-race]").forEach(b=>b.onclick=()=>openDetail(races[Number(b.dataset.race)]));show("races")}
 function raceFrom(m,raceNo){return{date:compact(state.date),venueCode:venueCode(m),venueName:m.venueName,raceNo,scheduledStart:"",raceCardUrl:getCard(m)?.url||"",oddsUrl:getOdds(m)?.url||""}}
 function openDetail(race){state.race={...race};state.payload=null;state.snapshot=findLatestSnapshot(localStorage,race);renderDetail();show("detail")}
-function renderDetail(){const r=state.race,s=raceStatus(r);$("detailDate").textContent=formatDate(r.date);$("detailTitle").textContent=`${r.venueName} ${r.raceNo}R`;$("raceStatus").textContent=s.label;$("raceStatus").className=`pill ${s.className}`;$("raceMeta").innerHTML=metas([["日付",formatDate(r.date)],["会場",r.venueName],["レース",`${r.raceNo}R`],["発走/締切",r.scheduledStart||"取得時に確認"]]);renderOfficial(state.payload);if(state.snapshot){$("savedPrediction").classList.remove("hidden");$("savedPrediction").innerHTML=`<div class="sectionHead"><h2>保存済み予想あり</h2><span class="pill success">${formatTime(state.snapshot.createdAt)}</span></div><p class="muted">以前保存した買い目を再表示できます。</p><button id="openSaved" class="secondary">保存済み予想を見る</button>`;$("openSaved").onclick=()=>renderPrediction(state.snapshot)}else $("savedPrediction").classList.add("hidden");$("predictBtn").disabled=false;$("predictBtn").textContent="このレースを予想"}
+function renderDetail(){const r=state.race,s=raceStatus(r);$("detailDate").textContent=formatDate(r.date);$("detailTitle").textContent=`${r.venueName} ${r.raceNo}R`;$("raceStatus").textContent=s.label;$("raceStatus").className=`pill ${s.className}`;$("raceMeta").innerHTML=metas([["日付",formatDate(r.date)],["会場",r.venueName],["レース",`${r.raceNo}R`],["発走/締切",r.scheduledStart||"取得時に確認"]]);renderOfficial(state.payload);renderPredictionDetail(state.snapshot);if(state.snapshot){$("savedPrediction").classList.remove("hidden");$("savedPrediction").innerHTML=`<div class="sectionHead"><h2>保存済み予想あり</h2><span class="pill success">${formatTime(state.snapshot.createdAt)}</span></div><p class="muted">買い目だけの画面へ戻れます。</p><button id="openSaved" class="secondary">買い目を見る</button>`;$("openSaved").onclick=()=>renderPrediction(state.snapshot)}else $("savedPrediction").classList.add("hidden");$("predictBtn").disabled=false;$("predictBtn").textContent="このレースを予想"}
 function renderOfficial(p){const race=p?.race;if(race){state.race={...state.race,scheduledStart:race.startTime||race.deadline||""};$("participantCount").textContent=`${race.participants.length}名`;$("participants").innerHTML=race.participants.map(x=>`<div class="rider"><span class="riderNo">${x.number}</span><span><strong>${esc(x.name)}</strong><small>ID ${esc(x.registration||x.sourcePath||"公式車番")}</small></span><small>${esc(x.className||x.prefecture||"")}</small></div>`).join("");$("officialLine").textContent=lineText(race.participants);$("oddsStatus").textContent=p.odds?.ok?`3連単 ${Object.keys(p.odds.odds||{}).length}件`:"オッズ取得失敗";$("oddsStatus").className=`pill ${p.odds?.ok?"success":"warning"}`;$("detailNotice").innerHTML=p.warnings?.length?`<div class="notice">${p.warnings.map(esc).join(" / ")}</div>`:""}else{$("participantCount").textContent="未取得";$("participants").innerHTML='<p class="muted">予想実行時に公式情報を取得します。</p>';$("officialLine").textContent="予想実行時に取得します";$("oddsStatus").textContent="オッズ未取得"}}
 
 async function predict(){if(state.busy)return;const fixed={...state.race,key:raceKey(state.race)};state.busy=true;$("predictBtn").disabled=true;setLoading("予想を作成中","出走選手・公式ライン・3連単オッズを取得しています。");try{const q=new URLSearchParams({date:fixed.date,venueName:fixed.venueName,venueCode:fixed.venueCode,raceCardUrl:fixed.raceCardUrl,raceNo:String(fixed.raceNo),budget:"3000"});if(fixed.oddsUrl)q.set("oddsUrl",fixed.oddsUrl);const p=await jsonFetch(`/.netlify/functions/keirin-predict?${q}`);if(raceKey(p.race)!==fixed.key)throw new Error("取得したレースが選択内容と一致しません");state.payload=p;state.race={...fixed,...p.race,venueName:p.race.venue};const snapshot=saveSnapshot(localStorage,createSnapshot(p));state.snapshot=snapshot;renderOfficial(p);renderPrediction(snapshot)}catch(e){fail("予想の取得・保存に失敗",e,predict)}finally{state.busy=false;$("predictBtn").disabled=false}}
-function renderPrediction(snapshot){state.snapshot=snapshot;const r=snapshot.targetRace;$("predictionTitle").textContent=`${r.venueName} ${r.raceNo}R`;$("recommendation").textContent=snapshot.noBet?"見送り":snapshot.recommendation||"予想結果";$("predictionUpdated").textContent=formatTime(snapshot.createdAt);$("predictionSummary").innerHTML=metas([["予想バージョン",snapshot.predictionVersion],["買い目",`${snapshot.betSelections.length}点`],["発走/締切",r.scheduledStart||"未取得"],["保存ID",snapshot.predictionSnapshotId.slice(-12)]]);const groups=[["本線","MAIN"],["押さえ","COVER"],["買える高配当","BUYABLE_HIGH"]];$("betGroups").innerHTML=groups.map(([label,key])=>{const bets=snapshot.betSelections.filter(b=>b.category===key);return bets.length?`<section class="betCard"><h3>${label}</h3><div class="betRows">${bets.map(b=>`<div class="betRow"><div><strong>${b.order.join("-")}</strong>${b.branchLabel?`<small>${esc(b.branchLabel)}${b.reason?` / ${esc(b.reason)}`:""}</small>`:""}${b.evidenceSummary?`<small class="evidenceTrace">${esc(b.evidenceSummary)}</small>`:""}</div><span>${b.odds?`${b.odds}倍`:"オッズ確認待ち"}${b.stake?` / ${Number(b.stake).toLocaleString()}円`:""}</span></div>`).join("")}</div></section>`:""}).join("")||(snapshot.noBet?`<section class="card empty"><strong>見送り</strong><p>${esc(noBetReasonText(snapshot.noBetReason))}</p></section>`:'<section class="card empty">購入対象の買い目はありません。</section>');renderResult(snapshot.result);show("prediction")}
+function renderPrediction(snapshot){state.snapshot=snapshot;const r=snapshot.targetRace;$("predictionTitle").textContent=`${r.venueName} ${r.raceNo}R`;$("recommendation").textContent=snapshot.noBet?"見送り":"買い目";$("predictionUpdated").textContent=formatTime(snapshot.createdAt);$("predictionSummary").innerHTML=metas([["買い目",`${snapshot.betSelections.length}点`],["発走/締切",r.scheduledStart||"未取得"]]);const groups=[["本線","MAIN"],["押さえ","COVER"],["買える高配当","BUYABLE_HIGH"]];$("betGroups").innerHTML=groups.map(([label,key])=>{const bets=snapshot.betSelections.filter(b=>b.category===key);return bets.length?`<section class="betCard"><h3>${label}</h3><div class="betRows">${bets.map(b=>`<div class="betRow betRowSimple"><strong>${b.order.join("-")}</strong><span>${b.odds?`${b.odds}倍`:"オッズ確認待ち"}${b.stake?` / ${Number(b.stake).toLocaleString()}円`:""}</span></div>`).join("")}</div></section>`:""}).join("")||(snapshot.noBet?`<section class="card empty"><strong>見送り</strong><p>${esc(noBetReasonText(snapshot.noBetReason))}</p></section>`:'<section class="card empty">購入対象の買い目はありません。</section>');renderResult(snapshot.result);show("prediction")}
+
+function renderPredictionDetail(snapshot){const panel=$("predictionDetail");if(!panel)return;if(!snapshot){panel.classList.add("hidden");panel.innerHTML="";return}const groups=[["本線","MAIN"],["押さえ","COVER"],["買える高配当","BUYABLE_HIGH"]];const betDetail=groups.map(([label,key])=>{const bets=(snapshot.betSelections||[]).filter(b=>b.category===key);if(!bets.length)return"";return `<div class="detailGroup"><h3>${label}</h3>${bets.map(b=>`<div class="detailBet"><strong>${b.order.join("-")}</strong>${b.branchLabel?`<p>${esc(b.branchLabel)}${b.reason?` / ${esc(b.reason)}`:""}</p>`:""}${b.evidenceSummary?`<p class="muted">${esc(b.evidenceSummary)}</p>`:""}</div>`).join("")}</div>`}).join("");const abilities=(snapshot.abilitiesUsed||[]).map(a=>`<div class="abilityRow"><strong>${a.number}番</strong><span>近況 ${fmtAbility(a.recentForm)} / 主導権 ${fmtAbility(a.startPower)} / まくり ${fmtAbility(a.sprintPower)} / 差し ${fmtAbility(a.finishPower)} / 追走 ${fmtAbility(a.trackingSkill)}</span></div>`).join("");panel.innerHTML=`<div class="sectionHead"><h2>予想詳細</h2><span class="pill">根拠</span></div>${betDetail||'<p class="muted">購入候補はありません。</p>'}${abilities?`<details class="abilityDetails"><summary>選手別能力を見る</summary><div class="abilityList">${abilities}</div></details>`:""}`;panel.classList.remove("hidden")}
+function fmtAbility(v){return Number.isFinite(Number(v))?Number(v).toFixed(2):"-"}
 function noBetReasonText(code){return({NO_TERMINALS:"展開候補を生成できませんでした。",FLAT_DISTRIBUTION_NO_SUPPORTED_CANDIDATE:"確率分布が平坦で、独立した展開根拠を持つ購入候補がありません。",BUDGET_BELOW_MINIMUM:"予算が最低購入単位を下回っています。",QUALITY_GATE:"データ品質基準を満たさないため購入を見送ります。",LINE_DATA_UNAVAILABLE:"公式ラインを確認できないため、通常の競輪予想としては購入判定を保留します。"})[code]||"購入価値を確認できないため見送ります。"}
 async function checkResult(){if(state.busy)return;state.busy=true;setLoading("公式結果を確認中","レースIDを固定して確定着順と払戻を取得しています。");try{const r=state.snapshot.targetRace,q=new URLSearchParams({date:r.date,venueCode:r.venueCode,venueName:r.venueName,raceNo:String(r.raceNo)}),p=await jsonFetch(`/.netlify/functions/keirin-result?${q}`);if(raceKey(p.race)!==raceKey(r))throw new Error("公式結果のレースが保存済み予想と一致しません");if(isResultPending(p.result)){renderPrediction(state.snapshot);renderPendingResult();return}state.snapshot=attachResult(localStorage,state.snapshot.predictionSnapshotId,p.result);renderPrediction(state.snapshot)}catch(e){fail("公式結果の取得・保存に失敗",e,checkResult)}finally{state.busy=false}}
 function renderPendingResult(){$("resultPanel").className="card";$("resultPanel").innerHTML='<div class="resultMark">結果はまだ確定していません</div><p class="muted">公式結果の確定後にもう一度確認してください。</p><button id="retryPendingResult" class="secondary">再試行</button>';$('retryPendingResult').onclick=()=>checkResult()}
diff -ruN /mnt/data/ui_v10_orig/chari-neko-dev-core-v9/public/index.html /mnt/data/ui_v10_work/chari-neko-dev-core-v9/public/index.html
--- /mnt/data/ui_v10_orig/chari-neko-dev-core-v9/public/index.html	2026-08-08 10:24:54.000000000 +0000
+++ /mnt/data/ui_v10_work/chari-neko-dev-core-v9/public/index.html	2026-08-08 14:18:46.639932549 +0000
@@ -37,6 +37,7 @@
     <section class="card compact"><div id="raceMeta" class="metaGrid"></div><div id="detailNotice"></div></section>
     <section class="card compact"><div class="sectionHead"><h2>出走選手</h2><span id="participantCount" class="pill">未取得</span></div><div id="participants" class="participants"><p class="muted">予想実行時に公式情報を取得します。</p></div></section>
     <section class="card compact"><div class="sectionHead"><h2>公式ライン</h2><span id="oddsStatus" class="pill">オッズ未取得</span></div><div id="officialLine" class="lineBox">予想実行時に取得します</div></section>
+    <section id="predictionDetail" class="card compact hidden"></section>
     <section id="savedPrediction" class="card compact hidden"></section>
     <div class="actionBar"><button id="predictBtn" class="primary">このレースを予想</button><button id="retryDetail" class="secondary hidden">再試行</button></div>
   </section>
diff -ruN /mnt/data/ui_v10_orig/chari-neko-dev-core-v9/public/styles.css /mnt/data/ui_v10_work/chari-neko-dev-core-v9/public/styles.css
--- /mnt/data/ui_v10_orig/chari-neko-dev-core-v9/public/styles.css	2026-08-08 11:32:47.000000000 +0000
+++ /mnt/data/ui_v10_work/chari-neko-dev-core-v9/public/styles.css	2026-08-08 14:18:46.640227920 +0000
@@ -1,3 +1,5 @@
 :root{color-scheme:dark;--bg:#08111f;--card:#132039;--inset:#0a1425;--line:#2a3b58;--text:#f8fafc;--muted:#94a3b8;--accent:#5bc0b4;--warning:#f59e0b;--danger:#f87171}*{box-sizing:border-box}html,body{margin:0;min-width:320px;background:var(--bg);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,input{font:inherit}button{min-height:44px;cursor:pointer}.hidden{display:none!important}.appHeader{position:sticky;top:0;z-index:20;height:calc(64px + env(safe-area-inset-top));padding:env(safe-area-inset-top) 12px 0;display:grid;grid-template-columns:48px 1fr 48px;align-items:center;background:#08111ff2;border-bottom:1px solid var(--line);backdrop-filter:blur(12px)}.brand,.iconBtn{border:0;background:transparent;color:var(--text)}.brand{display:flex;justify-content:center;align-items:center;gap:8px}.brand>span{display:grid;text-align:left}.brand strong{font-size:1.05rem}.brand small{color:var(--accent);font-size:.68rem}.brand>span:first-child{font-size:1.5rem}.iconBtn{font-size:1.4rem;border-radius:12px}.iconBtn:active{background:var(--card)}main{width:min(100%,760px);margin:auto;padding:16px 14px calc(92px + env(safe-area-inset-bottom))}.screen{display:none}.screen.active{display:block}.card{background:var(--card);border:1px solid var(--line);border-radius:18px;padding:16px;margin-bottom:14px}.card.compact{padding:14px}.hero{background:linear-gradient(145deg,#17304b,#132039)}.hero small,.pageTitle small,.sectionHead small{color:var(--accent);font-weight:700;letter-spacing:.08em}.hero h1,.pageTitle h1{font-size:1.45rem;margin:6px 0}.hero p,.muted{color:var(--muted)}.primary,.secondary{width:100%;border-radius:12px;padding:12px 14px;font-weight:800}.primary{border:0;background:var(--accent);color:#05201c}.primary:disabled{opacity:.55;cursor:wait}.secondary{border:1px solid var(--line);background:#17243a;color:var(--text);margin-top:8px}.pageTitle,.sectionHead,.meetingTop,.raceTop{display:flex;justify-content:space-between;align-items:center;gap:10px}.pageTitle{margin:4px 2px 14px}.pageTitle h1,.sectionHead h2{margin:0}.sectionHead h2{font-size:1rem}.pill{display:inline-flex;align-items:center;white-space:nowrap;border:1px solid var(--line);border-radius:999px;padding:5px 9px;font-size:.72rem}.pill.success{background:#083c36}.pill.warning{background:#713f12}.pill.danger{background:#7f1d1d}.filters{display:flex;gap:7px;overflow:auto;padding:10px 0 2px}.filter{border:1px solid var(--line);border-radius:999px;background:var(--inset);color:var(--muted);padding:7px 14px}.filter.active{background:#0f5048;color:#fff}.filter:disabled{opacity:.45}.stack{display:grid;gap:10px}.meeting,.savedItem,.betCard{border:1px solid var(--line);border-radius:16px;background:var(--card);padding:14px}.meeting h2,.raceCard h2,.savedItem h3,.betCard h3{margin:0}.meeting p,.raceCard p,.savedItem p{margin:7px 0;color:var(--muted)}.meeting button,.savedItem button{width:100%;border:1px solid var(--line);border-radius:11px;background:#17243a;color:#fff}.raceGrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.raceCard{min-width:0;border:1px solid var(--line);border-radius:16px;background:var(--card);padding:13px;text-align:left;color:#fff}.raceCard:active{border-color:var(--accent)}.raceCard h2{font-size:1.2rem}.raceCard p{font-size:.78rem;overflow-wrap:anywhere}.status{display:inline-block;font-size:.7rem;color:var(--accent)}label{display:grid;gap:6px;color:var(--muted);font-size:.8rem}input{min-height:44px;border:1px solid var(--line);border-radius:11px;background:var(--inset);color:#fff;padding:9px}.metaGrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.meta{min-width:0;background:var(--inset);border:1px solid var(--line);border-radius:12px;padding:10px}.meta span,.meta strong{display:block;overflow-wrap:anywhere}.meta span{font-size:.68rem;color:var(--muted)}.participants{display:grid;gap:7px;margin-top:10px}.rider{display:grid;grid-template-columns:38px 1fr auto;align-items:center;gap:9px;background:var(--inset);border-radius:11px;padding:9px}.riderNo{display:grid;place-items:center;width:34px;height:34px;background:#fff;color:#111;border-radius:8px;font-weight:900}.rider strong,.rider small{display:block}.rider small{color:var(--muted);font-size:.7rem}.lineBox{margin-top:10px;background:var(--inset);border-radius:12px;padding:12px;overflow-wrap:anywhere}.actionBar{position:fixed;z-index:15;bottom:0;left:0;right:0;display:flex;gap:8px;padding:10px max(14px,calc((100vw - 760px)/2 + 14px)) calc(10px + env(safe-area-inset-bottom));background:#08111ff2;border-top:1px solid var(--line);backdrop-filter:blur(12px)}.actionBar .secondary{margin:0}.betCard p{font-size:1.06rem;font-weight:800;line-height:1.65;margin:8px 0 0}.betRows{display:grid;gap:7px;margin-top:10px}.betRow{display:grid;grid-template-columns:1fr auto;gap:10px;background:var(--inset);border-radius:10px;padding:10px}.resultHit{border-color:#22c55e}.resultMiss{border-color:#ef4444}.resultMark{font-size:2.2rem;font-weight:900}.loadingCard,.errorCard{text-align:center;margin-top:14vh}.spinner{width:48px;height:48px;border:5px solid #334155;border-top-color:var(--accent);border-radius:50%;margin:5px auto 18px;animation:spin 1s linear infinite}.errorIcon{display:grid;place-items:center;width:48px;height:48px;margin:auto;border-radius:50%;background:#7f1d1d;font-size:1.6rem;font-weight:900}.empty{text-align:center;color:var(--muted);padding:20px}.notice{margin-top:10px;border-radius:10px;padding:10px;background:#713f12;color:#fef3c7;font-size:.8rem}@keyframes spin{to{transform:rotate(360deg)}}@media(min-width:600px){.raceGrid{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:390px){main{padding-left:10px;padding-right:10px}.card{padding:13px}.metaGrid{grid-template-columns:1fr}.pageTitle h1{font-size:1.28rem}.raceGrid{gap:8px}.raceCard{padding:11px}.actionBar{padding-left:10px;padding-right:10px}}
 
 .evidenceTrace{display:block;margin-top:4px;opacity:.78;font-size:.76rem;line-height:1.35}
+
+.betRowSimple{align-items:center}.betRowSimple strong{font-size:1.02rem}.detailGroup{margin-top:12px}.detailGroup h3{margin:0 0 8px;font-size:.92rem}.detailBet{background:var(--inset);border-radius:10px;padding:10px;margin-bottom:7px}.detailBet p{margin:5px 0 0;font-size:.78rem;line-height:1.45}.abilityDetails{margin-top:14px;border-top:1px solid var(--line);padding-top:12px}.abilityDetails summary{cursor:pointer;font-weight:800}.abilityList{display:grid;gap:7px;margin-top:10px}.abilityRow{display:grid;grid-template-columns:48px 1fr;gap:8px;background:var(--inset);border-radius:10px;padding:9px}.abilityRow span{color:var(--muted);font-size:.76rem;line-height:1.45}
