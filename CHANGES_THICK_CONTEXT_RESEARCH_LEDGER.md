# v141 厚め条件別研究台帳

- v140 の局所候補をグローバル候補と分離して研究台帳化。
- 会場・時間帯・レース格ごとの局所候補は `CONTEXT_RESEARCH_LEDGER_ONLY` として保存。
- 局所候補は全体ルール変更にも条件別本番補正にも自動接続しない。
- 次段階には独立条件再現、アウトオブサンプル検証、手動レビューを必須化。
- グローバル候補も `GLOBAL_RESEARCH_LEDGER_ONLY` とし、本番書込み禁止を維持。
