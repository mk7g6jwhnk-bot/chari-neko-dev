# v213 Extra Condition Provenance Alignment

- `追加条件 N件` を件数だけで監査せず、条件ID・段階・ラベル・必須性・機構・条件確率を保存/表示するようにした。
- `extraConditionAudit` を追加。購入終端の追加条件件数分布、確率レンジ、未校正構造条件、一律12%減点の偏りを監査する。
- 自然収束側のライン判定を修正。直後/3番手など限定位置だけでなく、同一ライン全メンバーを同ラインとして認識する。
- 同ラインの深位置選手を誤って `別線残り条件` とする不整合を解消した。
- 同ライン3番手→2着などはシナリオ整合度で差を付け、未校正の構造追加条件として二重減点しない。
- 真の別線残りは終端ノードの `otherLineRemain` 等の確率付き条件を追加条件として使用する。
- 購入ロジックの点数上限は追加していない。
