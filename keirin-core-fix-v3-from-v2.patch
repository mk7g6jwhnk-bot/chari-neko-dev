diff -ruN chari-neko-dev-core-v2/keirin/engine/keirin-engine.mjs chari-neko-dev-core-v3/keirin/engine/keirin-engine.mjs
--- chari-neko-dev-core-v2/keirin/engine/keirin-engine.mjs	2026-08-08 10:30:20.087310335 +0000
+++ chari-neko-dev-core-v3/keirin/engine/keirin-engine.mjs	2026-08-08 11:22:12.456214719 +0000
@@ -21,7 +21,7 @@
   if(lineBlocked){purchase.noBet=true;purchase.noBetReason="LINE_DATA_UNAVAILABLE";purchase.purchaseCandidateCountBeforeCompression=0;purchase.purchaseCandidateCountAfterCompression=0;purchase.finalBetCount=0;purchase.minimumRequired=0;}
 
   return{
-    engineVersion:"KEIRIN-0.4.2-branch-conditioned",
+    engineVersion:"KEIRIN-0.4.3-main-scenario-separated",
     raceId:race.id,
     lineConfidence:race.lineConfidence,
     scored,lines,branches,terminals:classified,
diff -ruN chari-neko-dev-core-v2/keirin/engine/purchase.mjs chari-neko-dev-core-v3/keirin/engine/purchase.mjs
--- chari-neko-dev-core-v2/keirin/engine/purchase.mjs	2026-08-08 11:10:40.072550375 +0000
+++ chari-neko-dev-core-v3/keirin/engine/purchase.mjs	2026-08-08 11:21:32.609847161 +0000
@@ -36,19 +36,25 @@
       const isAlternativeBranch=dominant.branchPriority!=="main";
       const highValue=hasOdds&&odd>=100&&credibleVariant&&probabilitySupported;
 
-      if(isMainBranch&&representative){
+      // MAIN is reserved for the single best terminal of each main-scenario branch.
+      // Keeping every representative terminal as MAIN recreates the old "everything is main" failure.
+      if(isMainBranch&&representative&&branchRank===1){
         betClass="MAIN";
         adopted=true;
-        purchaseReason=`${dominant.branchLabel}の代表終端`;
-      }else if(highValue&&isAlternativeBranch){
+        purchaseReason=`${dominant.branchLabel}の最上位代表終端`;
+      }else if(highValue&&isAlternativeBranch&&branchRank!=null&&branchRank<=2){
         betClass="BUYABLE_HIGH";
         adopted=true;
         purchaseReason=`${dominant.branchLabel}の独立展開から残る高配当候補`;
-      }else if((isMainBranch&&credibleVariant)||(isAlternativeBranch&&representative&&probabilitySupported)||(support>=2&&branchFit>=.90&&credibleVariant&&probabilitySupported)){
+      }else if(
+        (isMainBranch&&credibleVariant&&branchRank!=null&&branchRank<=3)||
+        (isAlternativeBranch&&representative&&probabilitySupported&&branchRank===1)||
+        (support>=2&&branchFit>=.90&&credibleVariant&&probabilitySupported&&branchRank!=null&&branchRank<=2)
+      ){
         betClass="COVER";
         adopted=true;
         purchaseReason=isMainBranch
-          ?`${dominant.branchLabel}の代表終端に近い着順変化`
+          ?`${dominant.branchLabel}の上位着順変化（枝内${branchRank}位）`
           :`${dominant.branchLabel}由来の別展開カバー`;
       }
     }
diff -ruN chari-neko-dev-core-v2/keirin/sports/keirin-branches.mjs chari-neko-dev-core-v3/keirin/sports/keirin-branches.mjs
--- chari-neko-dev-core-v2/keirin/sports/keirin-branches.mjs	2026-08-08 10:27:46.622893301 +0000
+++ chari-neko-dev-core-v3/keirin/sports/keirin-branches.mjs	2026-08-08 11:21:32.609223949 +0000
@@ -69,11 +69,17 @@
 
   const enabled=branches.filter(branch=>branch.firstCandidates.length&&branch.enabled).sort(compareBranch);
   const structured=enabled.filter(branch=>["LEADER_HOLD","BANTE_SASHI","MAKURI_SUCCESS"].includes(branch.branchType));
-  const bestStructured=structured[0]?.score||0;
+  const bestStructured=structured[0]||null;
+  const mainLineId=bestStructured?.primaryLineId||null;
+  const bestOnMainLine=mainLineId
+    ? Math.max(...structured.filter(branch=>branch.primaryLineId===mainLineId).map(branch=>branch.score),0)
+    : 0;
   return enabled.map(branch=>({
     ...branch,
+    // MAIN is the strongest line/scenario family, not every near-tied line.
+    // Other structured branches stay alive as SUB so their terminals can be COVER/value candidates.
     priority:structured.includes(branch)
-      ? (branch.score>=bestStructured*.92?"main":"sub")
+      ? (mainLineId&&branch.primaryLineId===mainLineId&&branch.score>=bestOnMainLine*.90?"main":"sub")
       : "risk"
   }));
 }
diff -ruN chari-neko-dev-core-v2/modules/keirin/engine/keirin-engine.mjs chari-neko-dev-core-v3/modules/keirin/engine/keirin-engine.mjs
--- chari-neko-dev-core-v2/modules/keirin/engine/keirin-engine.mjs	2026-08-08 10:30:20.102304800 +0000
+++ chari-neko-dev-core-v3/modules/keirin/engine/keirin-engine.mjs	2026-08-08 11:22:12.456440326 +0000
@@ -21,7 +21,7 @@
   if(lineBlocked){purchase.noBet=true;purchase.noBetReason="LINE_DATA_UNAVAILABLE";purchase.purchaseCandidateCountBeforeCompression=0;purchase.purchaseCandidateCountAfterCompression=0;purchase.finalBetCount=0;purchase.minimumRequired=0;}
 
   return{
-    engineVersion:"KEIRIN-0.4.2-branch-conditioned",
+    engineVersion:"KEIRIN-0.4.3-main-scenario-separated",
     raceId:race.id,
     lineConfidence:race.lineConfidence,
     scored,lines,branches,terminals:classified,
diff -ruN chari-neko-dev-core-v2/modules/keirin/sports/keirin-branches.mjs chari-neko-dev-core-v3/modules/keirin/sports/keirin-branches.mjs
--- chari-neko-dev-core-v2/modules/keirin/sports/keirin-branches.mjs	2026-08-08 10:27:46.643923709 +0000
+++ chari-neko-dev-core-v3/modules/keirin/sports/keirin-branches.mjs	2026-08-08 11:21:32.609521087 +0000
@@ -69,11 +69,17 @@
 
   const enabled=branches.filter(branch=>branch.firstCandidates.length&&branch.enabled).sort(compareBranch);
   const structured=enabled.filter(branch=>["LEADER_HOLD","BANTE_SASHI","MAKURI_SUCCESS"].includes(branch.branchType));
-  const bestStructured=structured[0]?.score||0;
+  const bestStructured=structured[0]||null;
+  const mainLineId=bestStructured?.primaryLineId||null;
+  const bestOnMainLine=mainLineId
+    ? Math.max(...structured.filter(branch=>branch.primaryLineId===mainLineId).map(branch=>branch.score),0)
+    : 0;
   return enabled.map(branch=>({
     ...branch,
+    // MAIN is the strongest line/scenario family, not every near-tied line.
+    // Other structured branches stay alive as SUB so their terminals can be COVER/value candidates.
     priority:structured.includes(branch)
-      ? (branch.score>=bestStructured*.92?"main":"sub")
+      ? (mainLineId&&branch.primaryLineId===mainLineId&&branch.score>=bestOnMainLine*.90?"main":"sub")
       : "risk"
   }));
 }
