# v0.5.2 開催日程の対象日セル判定

- 会場名リンクの存在を開催根拠にする処理を廃止。
- 月間日程表の各会場行について、指定日のセルだけを検査。
- 指定日セルにグレード画像（G/F等）がある会場だけを開催一覧へ採用。
- `colspan` を考慮して対象日のセルを決定。
- 「開催不可」等の案内画像だけのセルは除外。
- 日付＋会場コードで重複を除外。
- 診断情報に会場ごとの採用理由・画像根拠を保存。
