# KEIRIN-0.5.22-safe-auto-update

自動更新の再読込ループを防止した安全修正版。v44のライン順序修正・ガールズ主導権・一次選別・高速予想機能は維持。

- `APP_RELEASE` と `public/version.json` を同一バージョンへ同期。
- 同じリモート版への更新は1回だけ再読込し、同一版での無限リロードを禁止。
- URLの `appv` が既に対象版なら再読込しない。
- セッション内でも同じ対象版への再読込を重複実行しない。
- 更新成功後は `appv` クエリをURLから除去。
- バージョン不一致をCI/ローカルチェックで検出するテストを追加。
- 予想エンジン・ライン処理・一次選別ロジック・買い目判定は変更していない。
