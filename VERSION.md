# KEIRIN-0.5.21-light-primary-screening

一次選別・一括更新を個別予想の重い取得処理から分離した安定化版。ライン順序修正・ガールズ専用展開・一次選別UIは維持。

- 一括更新/一次選別は Railway `/keirin/preview` を使用。
- previewはプロフィール取得・全JSON探索を行わない。
- 一次選別の同時要求は1件に制限し、個別予想を塞ぎにくくする。
- 個別予想のRailway呼び出しは一時的な5xx/ブラウザ障害時に1回だけ再試行。
- 予想エンジン・終端生成・購入判定ロジックは変更していない。
