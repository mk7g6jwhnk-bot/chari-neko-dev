# v150 Third near-tie breadth

- Same 1-2 branch THIRD candidates are no longer collapsed to one rider merely because no natural probability gap exists.
- When the generic cluster has no meaningful separation, all THIRD candidates that independently pass `thirdFamilyNaturalEligible` and terminal `naturalConvergenceScore >= 0.46` remain selectable.
- Weak THIRD candidates still reach the dedicated bridge for audit but are not force-purchased.
- Adds `selectionMode` to third-purchase bridge audit rows.
