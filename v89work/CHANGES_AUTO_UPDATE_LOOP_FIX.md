# v45 自動更新ループ修正

v44では `public/app.mjs` の `APP_RELEASE` と `public/version.json` の version が不一致だったため、起動するたびに「新しい版がある」と誤判定して `location.replace()` を繰り返す状態になっていました。

v45では両者を同一バージョンへ同期し、さらに同一リモート版への再読込を1回に制限する安全装置を追加しました。今後バージョン記載を誤っても操作不能な無限更新には入りません。
