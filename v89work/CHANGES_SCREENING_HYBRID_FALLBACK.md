# v55 一次選別ハイブリッド化

- 一次選別は最低3R、最大6Rが揃った場合だけ成立扱い。
- 1〜2Rしか取得できない場合は順位・通常候補・高配当候補を表示しない。
- 取得数 / 成功数 / 失敗数を一次選別画面に表示。
- 通常候補と高配当寄りは排他的に分類し、同じレースを重複表示しない。
- 一次選別データは3Rずつ取得し、55秒を目安に打ち切る。
- 3R揃わない場合は締切順3Rの直接深掘り比較へ自動切替。
- 一次選別進捗キーをv4へ更新し、旧方式の途中状態を引き継がない。
- 予想エンジン・買い目生成ロジック自体はこの版では変更しない。
