# v42 ライン順序 + 一次選別

- 公式並びの `line-N` 識別子を使ってラインごとに分離し、`ichi` 順で先頭→番手→後位を確定。
- 詳細画面のライン表示を車番順ではなく `lineOrder` 順へ修正。
- ガールズは `1 / 2 / 3...` を固定ラインとして扱わず、主導権先行・まくりを選手別に生成。
- ガールズ詳細では主導権候補を表示。
- 一次選別タブ／ボタンを追加。未終了レースを浅く比較し、通常候補と高配当寄りを表示。
- 一次選別は買い目を生成せず、個別深掘りへ遷移するだけ。
