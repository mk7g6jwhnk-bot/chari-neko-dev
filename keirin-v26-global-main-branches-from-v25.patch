diff -ruN /mnt/data/v25_work/VERSION.md /mnt/data/v26_work/VERSION.md
--- /mnt/data/v25_work/VERSION.md	2026-08-09 02:25:04.000000000 +0000
+++ /mnt/data/v26_work/VERSION.md	2026-08-09 02:39:11.828232295 +0000
@@ -1,17 +1,12 @@
-# v25 branch-prior-audit
+# v26 global-main-branches
 
-- Prediction logic unchanged from v23 weighted branch support.
-- Adds audit-only visibility for pre-terminal branch scores, normalized branch share, top-relative score, priority, line id, and score trace.
-- Engine version: KEIRIN-0.5.3-branch-prior-audit.
+- Removes the single-main-line lock from Keirin branch selection.
+- Structured branches (先行押し切り / 番手差し / まくり) are compared across all official lines.
+- Any structured branch scoring at least 90% of the top structured branch is retained as a main-scenario candidate, regardless of line.
+- MAIN/COVER purchase logic is otherwise unchanged from v23; this isolates the line-lock correction for audit.
+- Branch audit now shows all main-scenario candidate branch labels and line ids.
+- Engine version: KEIRIN-0.5.4-global-main-branches.
 
-# Version
+# UI
 
-KEIRIN-0.5.2-weighted-branch-support / UI-v24-quick-navigation-deadlines
-
-- Prediction logic is unchanged from KEIRIN-0.5.2.
-- Added 会場選択 / レース選択 quick buttons beside the back button.
-- Venue cards show the nearest upcoming cutoff time when discover data provides official race times.
-- Race cards and timeline rows show cutoff time explicitly.
-- Venue cards are color-coded by モーニング / デイ / ナイター / ミッド.
-- Saved race snapshots preserve the official deadline separately when available.
-- Netlify discover adapter accepts Railway v0.5.6 per-race deadline/startTime metadata.
+- UI remains v24 quick-navigation/deadlines plus v25 branch-prior audit.
diff -ruN /mnt/data/v25_work/keirin/engine/keirin-engine.mjs /mnt/data/v26_work/keirin/engine/keirin-engine.mjs
--- /mnt/data/v25_work/keirin/engine/keirin-engine.mjs	2026-08-09 02:25:04.000000000 +0000
+++ /mnt/data/v26_work/keirin/engine/keirin-engine.mjs	2026-08-09 02:39:11.827196411 +0000
@@ -21,7 +21,7 @@
   if(lineBlocked){purchase.noBet=true;purchase.noBetReason="LINE_DATA_UNAVAILABLE";purchase.purchaseCandidateCountBeforeCompression=0;purchase.purchaseCandidateCountAfterCompression=0;purchase.finalBetCount=0;purchase.minimumRequired=0;}
 
   return{
-    engineVersion:"KEIRIN-0.5.3-branch-prior-audit",
+    engineVersion:"KEIRIN-0.5.4-global-main-branches",
     raceId:race.id,
     lineConfidence:race.lineConfidence,
     scored,lines,branches,terminals:classified,
@@ -51,8 +51,8 @@
   const totalScore=sorted.reduce((sum,branch)=>sum+(Number(branch.score)||0),0);
   const structured=sorted.filter(branch=>["LEADER_HOLD","BANTE_SASHI","MAKURI_SUCCESS"].includes(branch.branchType));
   const topStructured=structured[0]||null;
-  const mainLineId=topStructured?.primaryLineId||null;
-  const mainLineBestScore=mainLineId?Math.max(...structured.filter(branch=>branch.primaryLineId===mainLineId).map(branch=>Number(branch.score)||0),0):0;
+  const topStructuredScore=Number(topStructured?.score)||0;
+  const mainBranches=structured.filter(branch=>branch.priority==="main");
   const topScore=Number(sorted[0]?.score)||0;
   return{
     totalBranchScore:totalScore,
@@ -61,8 +61,12 @@
     topBranchScore:topScore,
     topStructuredBranchId:topStructured?.id||null,
     topStructuredBranchLabel:topStructured?.label||null,
-    mainLineId,
-    mainLineBestScore,
+    topStructuredScore,
+    mainSelectionMode:"GLOBAL_STRUCTURED_SCORE_BAND",
+    mainLineId:null,
+    mainLineIds:[...new Set(mainBranches.map(branch=>branch.primaryLineId).filter(Boolean))],
+    mainBranchIds:mainBranches.map(branch=>branch.id),
+    mainBranchLabels:mainBranches.map(branch=>branch.label),
     mainPriorityRatio:.90,
     rows:sorted.map(branch=>({
       id:branch.id,
diff -ruN /mnt/data/v25_work/keirin/sports/keirin-branches.mjs /mnt/data/v26_work/keirin/sports/keirin-branches.mjs
--- /mnt/data/v25_work/keirin/sports/keirin-branches.mjs	2026-08-08 13:17:20.000000000 +0000
+++ /mnt/data/v26_work/keirin/sports/keirin-branches.mjs	2026-08-09 02:39:11.827012360 +0000
@@ -36,12 +36,13 @@
 
   const enabled=branches.filter(branch=>branch.firstCandidates.length&&branch.enabled).sort(compareBranch);
   const structured=enabled.filter(branch=>["LEADER_HOLD","BANTE_SASHI","MAKURI_SUCCESS"].includes(branch.branchType));
-  const bestStructured=structured[0]||null;
-  const mainLineId=bestStructured?.primaryLineId||null;
-  const bestOnMainLine=mainLineId?Math.max(...structured.filter(branch=>branch.primaryLineId===mainLineId).map(branch=>branch.score),0):0;
+  const bestStructuredScore=structured[0]?.score||0;
+  const mainScoreFloor=bestStructuredScore*.90;
   return enabled.map(branch=>({
     ...branch,
-    priority:structured.includes(branch)?(mainLineId&&branch.primaryLineId===mainLineId&&branch.score>=bestOnMainLine*.90?"main":"sub"):"risk"
+    // Main-scenario candidates are compared across every official line.
+    // Do not lock the race to the line that happens to own the top branch.
+    priority:structured.includes(branch)&&(bestStructuredScore>0&&branch.score>=mainScoreFloor)?"main":structured.includes(branch)?"sub":"risk"
   }));
 }
 
diff -ruN /mnt/data/v25_work/package.json /mnt/data/v26_work/package.json
--- /mnt/data/v25_work/package.json	2026-08-09 02:25:04.000000000 +0000
+++ /mnt/data/v26_work/package.json	2026-08-09 02:39:11.828191309 +0000
@@ -4,7 +4,7 @@
   "private": true,
   "type": "module",
   "scripts": {
-    "test": "node modules/keirin/tests/run-tests.mjs && node tests/prediction-flow.mjs && node tests/keirin-purchase-five-races.mjs && node tests/keirin-purchase-no-rank-cap.mjs && node tests/keirin-purchase-audit.mjs && node tests/keirin-purchase-no-probability-cutoff.mjs && node tests/keirin-official-evidence-branching.mjs && node tests/keirin-branch-role-semantics.mjs && node tests/keirin-provenance-consistency.mjs && node tests/keirin-support-branch-audit.mjs && node tests/keirin-weighted-branch-support.mjs && node tests/keirin-branch-prior-audit.mjs",
+    "test": "node modules/keirin/tests/run-tests.mjs && node tests/prediction-flow.mjs && node tests/keirin-purchase-five-races.mjs && node tests/keirin-purchase-no-rank-cap.mjs && node tests/keirin-purchase-audit.mjs && node tests/keirin-purchase-no-probability-cutoff.mjs && node tests/keirin-official-evidence-branching.mjs && node tests/keirin-branch-role-semantics.mjs && node tests/keirin-provenance-consistency.mjs && node tests/keirin-support-branch-audit.mjs && node tests/keirin-weighted-branch-support.mjs && node tests/keirin-branch-prior-audit.mjs && node tests/keirin-global-main-branches.mjs",
     "check": "node --check public/app.mjs && node --check public/prediction-store.mjs && node --check netlify/functions/keirin-result.mjs && node --check netlify/functions/keirin-predict.mjs && node --check netlify/functions/keirin-odds.mjs"
   },
   "dependencies": {
diff -ruN /mnt/data/v25_work/public/app.mjs /mnt/data/v26_work/public/app.mjs
--- /mnt/data/v25_work/public/app.mjs	2026-08-09 02:25:04.000000000 +0000
+++ /mnt/data/v26_work/public/app.mjs	2026-08-09 02:39:11.827917669 +0000
@@ -53,7 +53,7 @@
 function allocatePreviewStakes(bets,budget,mode){const n=bets.length,min=n*100;if(!n||budget<min)return null;const base=bets.map(b=>Math.max(1,Number(b.stake)||100));const mul=bets.map(b=>mode==="main"?(b.category==="MAIN"?1.6:b.category==="BUYABLE_HIGH"?.8:1):mode==="high"?(b.category==="BUYABLE_HIGH"?1.6:b.category==="MAIN"?1:.9):1);const weights=base.map((v,i)=>v*mul[i]),extra=budget-min,total=weights.reduce((a,b)=>a+b,0)||n;let out=weights.map(w=>100+Math.floor((extra*w/total)/100)*100),used=out.reduce((a,b)=>a+b,0),remain=Math.floor((budget-used)/100);const order=weights.map((w,i)=>({w,i})).sort((a,b)=>b.w-a.w||a.i-b.i);for(let k=0;k<remain;k++)out[order[k%order.length].i]+=100;return out}
 function calcCompositeOdds(bets){const valid=bets.map(b=>Number(b.odds)).filter(v=>Number.isFinite(v)&&v>0);if(!valid.length||valid.length!==bets.length)return null;const inv=valid.reduce((s,v)=>s+1/v,0);return inv>0?1/inv:null}
 
-function renderBranchSelectionAudit(audit){const data=audit?.branchSelectionAudit;if(!data||!Array.isArray(data.rows)||!data.rows.length)return"";const priorityLabel={main:"主展開",sub:"別展開",risk:"リスク枝"};const rows=data.rows.map((branch,index)=>{const trace=Array.isArray(branch.scoreTrace)?branch.scoreTrace:[];const traceText=trace.slice(0,5).map(item=>`${esc(item.key)} ${fmtRatio(item.value)}×${fmtRatio(item.weight)}=${fmtRatio(item.contribution)}`).join(" / ");return `<div class="detailBet"><strong>${index+1}. ${esc(branch.label||branch.id||"不明")}　${esc(priorityLabel[branch.priority]||branch.priority||"-")}</strong><p>枝スコア ${fmtRatio(branch.score)} / 全枝比 ${fmtPct(branch.share)} / 首位比 ${fmtPct(branch.relativeToTop)}${branch.primaryLineId?` / ライン ${esc(branch.primaryLineId)}`:""}</p>${traceText?`<p class="muted">内訳: ${traceText}</p>`:""}</div>`}).join("");const basis=data.topStructuredBranchLabel?`主展開ラインは、構造枝の首位「${esc(data.topStructuredBranchLabel)}」が属する ${esc(data.mainLineId||"-")} を基準に選択。同ライン内は首位枝スコアの90%以上を主展開扱い。`:"構造枝の主展開候補なし。";return `<h3>展開枝の事前評価</h3><div class="auditCallout"><strong>主展開選択の基準</strong><p>${basis}</p></div><div class="detailGroup">${rows}</div>`}
+function renderBranchSelectionAudit(audit){const data=audit?.branchSelectionAudit;if(!data||!Array.isArray(data.rows)||!data.rows.length)return"";const priorityLabel={main:"主展開",sub:"別展開",risk:"リスク枝"};const rows=data.rows.map((branch,index)=>{const trace=Array.isArray(branch.scoreTrace)?branch.scoreTrace:[];const traceText=trace.slice(0,5).map(item=>`${esc(item.key)} ${fmtRatio(item.value)}×${fmtRatio(item.weight)}=${fmtRatio(item.contribution)}`).join(" / ");return `<div class="detailBet"><strong>${index+1}. ${esc(branch.label||branch.id||"不明")}　${esc(priorityLabel[branch.priority]||branch.priority||"-")}</strong><p>枝スコア ${fmtRatio(branch.score)} / 全枝比 ${fmtPct(branch.share)} / 首位比 ${fmtPct(branch.relativeToTop)}${branch.primaryLineId?` / ライン ${esc(branch.primaryLineId)}`:""}</p>${traceText?`<p class="muted">内訳: ${traceText}</p>`:""}</div>`}).join("");const basis=data.topStructuredBranchLabel?`構造枝を全ライン横断で比較。首位「${esc(data.topStructuredBranchLabel)}」の枝スコアに対して90%以上の枝を、ラインを固定せず主展開候補として保持。主展開候補: ${(data.mainBranchLabels||[]).map(esc).join(" / ")||"なし"}。`:"構造枝の主展開候補なし。";return `<h3>展開枝の事前評価</h3><div class="auditCallout"><strong>主展開選択の基準</strong><p>${basis}</p></div><div class="detailGroup">${rows}</div>`}
 function renderAdoptedTerminalAudit(audit){const rows=Array.isArray(audit?.adoptedTerminalAudit)?audit.adoptedTerminalAudit:[];if(!rows.length)return"";const branchRows=Object.entries(audit.adoptedBranchCounts||{}).sort((a,b)=>b[1]-a[1]).map(([label,count])=>`<div class="abilityRow"><strong>${esc(label)}</strong><span>${count}件</span></div>`).join("");const terminalRows=rows.map(item=>{const r=item.decisionRatios||{};const ratios=`1着 ${fmtRatio(r.first)} / 2着 ${fmtRatio(r.second)} / 3着 ${fmtRatio(r.third)}`;const supportBranches=Array.isArray(item.supportBranches)?item.supportBranches:[];const supportRows=supportBranches.map((branch,index)=>`<div class="abilityRow"><strong>${index+1}. ${esc(branch.branchLabel||branch.branchId||"不明")}</strong><span>${esc(branch.branchPriority||"-")} / 寄与 ${fmtRatio(branch.probability)} / 枝内適合 ${fmtRatio(branch.withinBranchFit)} / 枝強度 ${fmtRatio(branch.branchStrengthRatio)} / 加重 ${fmtRatio(branch.weightedSupport)}</span></div>`).join("");const dup=Array.isArray(item.duplicateSupportLabels)&&item.duplicateSupportLabels.length?`<p class="auditWarn">重複ラベル: ${item.duplicateSupportLabels.map(x=>`${esc(x.label)}×${x.count}`).join(" / ")}</p>`:"";return `<div class="detailBet"><strong>${esc(item.order)}　${esc(item.betClass||"")}</strong><p>${esc(item.dominantBranchLabel||"由来枝不明")} / 枝適合 ${fmtRatio(item.branchFit)} / 枝内${item.branchRank??"-"}位 / 由来枝${item.branchSupport??0}（固有${item.uniqueSupportBranchCount??item.branchSupport??0}） / 加重支持 ${fmtRatio(item.weightedBranchSupport)}</p><p class="muted">${ratios}</p>${supportRows?`<details class="supportBranchAudit"><summary>支持枝を全部見る（${supportBranches.length}本）</summary><div class="abilityList">${supportRows}</div>${dup}</details>`:""}</div>`}).join("");return `<h3>採用終端の由来</h3>${branchRows?`<div class="abilityList">${branchRows}</div>`:""}<div class="detailGroup">${terminalRows}</div>`}
 function fmtRatio(value){const n=Number(value);return Number.isFinite(n)?n.toFixed(3):"-"}
 function fmtPct(v){const n=Number(v);return Number.isFinite(n)?`${(n*100).toFixed(1)}%`:"-"}
diff -ruN /mnt/data/v25_work/tests/keirin-branch-prior-audit.mjs /mnt/data/v26_work/tests/keirin-branch-prior-audit.mjs
--- /mnt/data/v25_work/tests/keirin-branch-prior-audit.mjs	2026-08-09 02:25:04.000000000 +0000
+++ /mnt/data/v26_work/tests/keirin-branch-prior-audit.mjs	2026-08-09 02:39:11.828024224 +0000
@@ -2,4 +2,4 @@
 import handler from "../netlify/functions/keirin-predict.mjs";
 const originalFetch=globalThis.fetch;process.env.KEIRIN_BROWSER_SERVICE_URL="https://railway.test";
 globalThis.fetch=async()=>new Response(JSON.stringify({ok:true,officialData:{basic:{venueName:"立川",date:"2026/08/09",raceNo:3,startTime:"11:30"},participants:Array.from({length:7},(_,i)=>({number:i+1,registration:`28${String(i+1).padStart(4,"0")}`,name:`選手${i+1}`,score:90+i,escapeCount:i%3,makuriCount:(i+1)%4,differenceCount:i%2,markCount:(i+2)%3,backCount:(i*2)%5})),lines:[{number:1,lineId:"A",position:1},{number:2,lineId:"A",position:2},{number:3,lineId:"B",position:1},{number:4,lineId:"B",position:2},{number:5,lineId:"C",position:1},{number:6,lineId:"C",position:2},{number:7,lineId:"C",position:3}],odds:{ok:false,odds:{}}},audit:{identityPassed:true}}),{status:200,headers:{"content-type":"application/json"}});
-try{const r=await handler(new Request("https://test/.netlify/functions/keirin-predict?date=20260809&venueCode=28&venueName=%E7%AB%8B%E5%B7%9D&raceNo=3&budget=3000"));const p=await r.json();assert.equal(p.ok,true);const a=p.prediction.audit.branchSelectionAudit;assert.ok(a);assert.ok(a.rows.length>=3);assert.ok(a.topStructuredBranchLabel);assert.ok(a.mainLineId);assert.ok(Math.abs(a.rows.reduce((sum,row)=>sum+row.share,0)-1)<1e-9);assert.ok(a.rows.every(row=>Number.isFinite(row.score)&&Number.isFinite(row.relativeToTop)&&Array.isArray(row.scoreTrace)));console.log("Keirin branch prior audit passed:",a.topStructuredBranchLabel,a.mainLineId,a.rows.length);}finally{globalThis.fetch=originalFetch}
+try{const r=await handler(new Request("https://test/.netlify/functions/keirin-predict?date=20260809&venueCode=28&venueName=%E7%AB%8B%E5%B7%9D&raceNo=3&budget=3000"));const p=await r.json();assert.equal(p.ok,true);const a=p.prediction.audit.branchSelectionAudit;assert.ok(a);assert.ok(a.rows.length>=3);assert.ok(a.topStructuredBranchLabel);assert.equal(a.mainSelectionMode,"GLOBAL_STRUCTURED_SCORE_BAND");assert.ok(Array.isArray(a.mainLineIds));assert.ok(Array.isArray(a.mainBranchIds));assert.ok(a.mainBranchIds.length>=1);assert.ok(Math.abs(a.rows.reduce((sum,row)=>sum+row.share,0)-1)<1e-9);assert.ok(a.rows.every(row=>Number.isFinite(row.score)&&Number.isFinite(row.relativeToTop)&&Array.isArray(row.scoreTrace)));console.log("Keirin branch prior audit passed:",a.topStructuredBranchLabel,a.mainLineIds.join(","),a.rows.length);}finally{globalThis.fetch=originalFetch}
diff -ruN /mnt/data/v25_work/tests/keirin-global-main-branches.mjs /mnt/data/v26_work/tests/keirin-global-main-branches.mjs
--- /mnt/data/v25_work/tests/keirin-global-main-branches.mjs	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/v26_work/tests/keirin-global-main-branches.mjs	2026-08-09 02:39:11.828134057 +0000
@@ -0,0 +1,28 @@
+import assert from "node:assert/strict";
+import {generateKeirinBranches} from "../keirin/sports/keirin-branches.mjs";
+
+function rider(id,number,{first=5,start=5,sprint=5,finish=5,tracking=5,recent=5,role="ライン"}={}){
+  return {id,number,role,roleScores:{first},evidence:{start,sprint,finish,tracking,recent}};
+}
+const a1=rider("a1",1,{first:5.8,start:5.5,sprint:5.2,finish:5.0,recent:5.5});
+const a2=rider("a2",2,{first:8.0,finish:8.0,tracking:8.0,recent:8.0});
+const b1=rider("b1",3,{first:5.5,start:5.0,sprint:5.0,finish:5.0,recent:5.0});
+const b2=rider("b2",4,{first:5.4,finish:5.1,tracking:5.1,recent:5.0});
+const c1=rider("c1",5,{first:8.5,start:6.0,sprint:9.0,finish:8.0,recent:8.0});
+const c2=rider("c2",6,{first:5.5,finish:5.0,tracking:5.5,recent:5.0});
+const scored=[a1,a2,b1,b2,c1,c2];
+const lines=[
+  {id:"A",type:"ライン",leader:a1,bante:a2},
+  {id:"B",type:"ライン",leader:b1,bante:b2},
+  {id:"C",type:"ライン",leader:c1,bante:c2}
+];
+const branches=generateKeirinBranches({scored,lines,lineConfidence:"高"});
+const cMakuri=branches.find(branch=>branch.id==="MAKURI-C");
+const aBante=branches.find(branch=>branch.id==="BANTE-A");
+const bMakuri=branches.find(branch=>branch.id==="MAKURI-B");
+assert.ok(cMakuri&&aBante&&bMakuri);
+assert.equal(cMakuri.priority,"main");
+assert.equal(aBante.priority,"main","a near-top branch on another line must remain a main-scenario candidate");
+assert.notEqual(cMakuri.primaryLineId,aBante.primaryLineId);
+assert.equal(bMakuri.priority,"sub");
+console.log("Keirin global main branches passed:",cMakuri.label,cMakuri.score.toFixed(3),aBante.label,aBante.score.toFixed(3));
