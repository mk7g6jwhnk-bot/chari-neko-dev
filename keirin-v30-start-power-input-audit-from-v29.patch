diff -ruN /mnt/data/v29orig/VERSION.md /mnt/data/v29repo/VERSION.md
--- /mnt/data/v29orig/VERSION.md	2026-08-09 03:15:40.000000000 +0000
+++ /mnt/data/v29repo/VERSION.md	2026-08-09 03:25:54.223831906 +0000
@@ -1,3 +1,9 @@
+# KEIRIN-0.5.8-start-power-input-audit
+
+- 予想ロジックは0.5.7から変更なし。
+- 保存済み予想に startPowerEvidence を保持。
+- レース詳細に主導権入力監査（B/H/出走数/生頻度/縮小後頻度/latentScore/最終値/confidence）を追加。
+
 # チャリ猫 Branch Deploy
 
 - UI bundle: v29
diff -ruN /mnt/data/v29orig/keirin/engine/keirin-engine.mjs /mnt/data/v29repo/keirin/engine/keirin-engine.mjs
--- /mnt/data/v29orig/keirin/engine/keirin-engine.mjs	2026-08-09 03:15:40.000000000 +0000
+++ /mnt/data/v29repo/keirin/engine/keirin-engine.mjs	2026-08-09 03:25:54.223717110 +0000
@@ -21,7 +21,7 @@
   if(lineBlocked){purchase.noBet=true;purchase.noBetReason="LINE_DATA_UNAVAILABLE";purchase.purchaseCandidateCountBeforeCompression=0;purchase.purchaseCandidateCountAfterCompression=0;purchase.finalBetCount=0;purchase.minimumRequired=0;}
 
   return{
-    engineVersion:"KEIRIN-0.5.7-start-power-single-shrink",
+    engineVersion:"KEIRIN-0.5.8-start-power-input-audit",
     raceId:race.id,
     lineConfidence:race.lineConfidence,
     scored,lines,branches,terminals:classified,
diff -ruN /mnt/data/v29orig/package.json /mnt/data/v29repo/package.json
--- /mnt/data/v29orig/package.json	2026-08-09 03:15:40.000000000 +0000
+++ /mnt/data/v29repo/package.json	2026-08-09 03:25:55.444572927 +0000
@@ -4,7 +4,7 @@
   "private": true,
   "type": "module",
   "scripts": {
-    "test": "node modules/keirin/tests/run-tests.mjs && node tests/prediction-flow.mjs && node tests/keirin-purchase-five-races.mjs && node tests/keirin-purchase-no-rank-cap.mjs && node tests/keirin-purchase-audit.mjs && node tests/keirin-purchase-no-probability-cutoff.mjs && node tests/keirin-official-evidence-branching.mjs && node tests/keirin-branch-role-semantics.mjs && node tests/keirin-provenance-consistency.mjs && node tests/keirin-support-branch-audit.mjs && node tests/keirin-weighted-branch-support.mjs && node tests/keirin-branch-prior-audit.mjs && node tests/keirin-global-main-branches.mjs && node tests/keirin-adaptive-main-cluster.mjs && node tests/keirin-start-power-single-shrink.mjs",
+    "test": "node modules/keirin/tests/run-tests.mjs && node tests/prediction-flow.mjs && node tests/keirin-purchase-five-races.mjs && node tests/keirin-purchase-no-rank-cap.mjs && node tests/keirin-purchase-audit.mjs && node tests/keirin-purchase-no-probability-cutoff.mjs && node tests/keirin-official-evidence-branching.mjs && node tests/keirin-branch-role-semantics.mjs && node tests/keirin-provenance-consistency.mjs && node tests/keirin-support-branch-audit.mjs && node tests/keirin-weighted-branch-support.mjs && node tests/keirin-branch-prior-audit.mjs && node tests/keirin-global-main-branches.mjs && node tests/keirin-adaptive-main-cluster.mjs && node tests/keirin-start-power-single-shrink.mjs && node tests/keirin-start-power-input-audit.mjs",
     "check": "node --check public/app.mjs && node --check public/prediction-store.mjs && node --check netlify/functions/keirin-result.mjs && node --check netlify/functions/keirin-predict.mjs && node --check netlify/functions/keirin-odds.mjs"
   },
   "dependencies": {
diff -ruN /mnt/data/v29orig/public/app.mjs /mnt/data/v29repo/public/app.mjs
--- /mnt/data/v29orig/public/app.mjs	2026-08-09 03:03:52.000000000 +0000
+++ /mnt/data/v29repo/public/app.mjs	2026-08-09 03:25:54.827173856 +0000
@@ -57,7 +57,10 @@
 function renderAdoptedTerminalAudit(audit){const rows=Array.isArray(audit?.adoptedTerminalAudit)?audit.adoptedTerminalAudit:[];if(!rows.length)return"";const branchRows=Object.entries(audit.adoptedBranchCounts||{}).sort((a,b)=>b[1]-a[1]).map(([label,count])=>`<div class="abilityRow"><strong>${esc(label)}</strong><span>${count}件</span></div>`).join("");const terminalRows=rows.map(item=>{const r=item.decisionRatios||{};const ratios=`1着 ${fmtRatio(r.first)} / 2着 ${fmtRatio(r.second)} / 3着 ${fmtRatio(r.third)}`;const supportBranches=Array.isArray(item.supportBranches)?item.supportBranches:[];const supportRows=supportBranches.map((branch,index)=>`<div class="abilityRow"><strong>${index+1}. ${esc(branch.branchLabel||branch.branchId||"不明")}</strong><span>${esc(branch.branchPriority||"-")} / 寄与 ${fmtRatio(branch.probability)} / 枝内適合 ${fmtRatio(branch.withinBranchFit)} / 枝強度 ${fmtRatio(branch.branchStrengthRatio)} / 加重 ${fmtRatio(branch.weightedSupport)}</span></div>`).join("");const dup=Array.isArray(item.duplicateSupportLabels)&&item.duplicateSupportLabels.length?`<p class="auditWarn">重複ラベル: ${item.duplicateSupportLabels.map(x=>`${esc(x.label)}×${x.count}`).join(" / ")}</p>`:"";return `<div class="detailBet"><strong>${esc(item.order)}　${esc(item.betClass||"")}</strong><p>${esc(item.dominantBranchLabel||"由来枝不明")} / 枝適合 ${fmtRatio(item.branchFit)} / 枝内${item.branchRank??"-"}位 / 由来枝${item.branchSupport??0}（固有${item.uniqueSupportBranchCount??item.branchSupport??0}） / 加重支持 ${fmtRatio(item.weightedBranchSupport)}</p><p class="muted">${ratios}</p>${supportRows?`<details class="supportBranchAudit"><summary>支持枝を全部見る（${supportBranches.length}本）</summary><div class="abilityList">${supportRows}</div>${dup}</details>`:""}</div>`}).join("");return `<h3>採用終端の由来</h3>${branchRows?`<div class="abilityList">${branchRows}</div>`:""}<div class="detailGroup">${terminalRows}</div>`}
 function fmtRatio(value){const n=Number(value);return Number.isFinite(n)?n.toFixed(3):"-"}
 function fmtPct(v){const n=Number(v);return Number.isFinite(n)?`${(n*100).toFixed(1)}%`:"-"}
-function renderPredictionDetail(snapshot){const panel=$("predictionDetail");if(!panel)return;if(!snapshot){panel.classList.add("hidden");panel.innerHTML="";return}const groups=[["本線","MAIN"],["押さえ","COVER"],["買える高配当","BUYABLE_HIGH"]];const betDetail=groups.map(([label,key])=>{const bets=(snapshot.betSelections||[]).filter(b=>b.category===key);if(!bets.length)return"";return `<div class="detailGroup"><h3>${label}</h3>${bets.map(b=>`<div class="detailBet"><strong>${b.order.join("-")}</strong>${b.branchLabel?`<p>${esc(b.branchLabel)}${b.reason?` / ${esc(b.reason)}`:""}</p>`:""}${b.evidenceSummary?`<p class="muted">${esc(b.evidenceSummary)}</p>`:""}</div>`).join("")}</div>`}).join("");const abilities=(snapshot.abilitiesUsed||[]).map(a=>`<div class="abilityRow"><strong>${a.number}番</strong><span>近況 ${fmtAbility(a.recentForm)} / 主導権 ${fmtAbility(a.startPower)} / まくり ${fmtAbility(a.sprintPower)} / 差し ${fmtAbility(a.finishPower)} / 追走 ${fmtAbility(a.trackingSkill)}</span></div>`).join("");const audit=snapshot.predictionOutput?.audit||{};const rejectLabels={FLAT_DISTRIBUTION:"分布が平坦",NO_DOMINANT_BRANCH:"支配展開なし",PROBABILITY_SUPPORT:"確率支持不足",BRANCH_OR_POSITION_SUPPORT:"枝適合/着順支持不足",CLASS_RULE:"分類条件不成立",UNKNOWN:"その他"};const rejectRows=Object.entries(audit.rejectCodeCounts||{}).sort((a,b)=>b[1]-a[1]).map(([code,count])=>`<div class="abilityRow"><strong>${esc(rejectLabels[code]||code)}</strong><span>${count}件</span></div>`).join("");const auditDetail=Number.isFinite(audit.generatedTerminalCount)?`<details class="predictionAccordion" open><summary>購入採用監査を見る</summary><div class="accordionBody"><div class="abilityList"><div class="abilityRow"><strong>生成終端</strong><span>${audit.generatedTerminalCount}件</span></div><div class="abilityRow"><strong>確率評価済み</strong><span>${audit.probabilityEvaluatedTerminalCount??audit.terminalCount??0}件</span></div><div class="abilityRow"><strong>購入採用</strong><span>${audit.adoptedTerminalCount??audit.finalBetCount??0}件</span></div><div class="abilityRow"><strong>購入不採用</strong><span>${audit.rejectedTerminalCount??0}件</span></div></div>${rejectRows?`<h3>不採用理由</h3><div class="abilityList">${rejectRows}</div>`:""}${renderBranchSelectionAudit(audit)}${renderAdoptedTerminalAudit(audit)}<p class="muted">固定点数・枝内順位上限・確率比の一律足切りは使用していません。枝数そのものは購入支持に使わず、枝強度×枝内適合の加重支持で監査します。</p></div></details>`:"";panel.innerHTML=`<div class="sectionHead"><div><small>保存済み予想の根拠</small><h2>予想詳細</h2></div><span class="pill">詳細</span></div>${auditDetail}<details class="predictionAccordion"><summary>展開・買い目の根拠を見る</summary><div class="accordionBody">${betDetail||'<p class="muted">購入候補はありません。</p>'}</div></details>${abilities?`<details class="predictionAccordion"><summary>選手別能力を見る</summary><div class="abilityList accordionBody">${abilities}</div></details>`:""}`;panel.classList.remove("hidden")}
+function renderPredictionDetail(snapshot){const panel=$("predictionDetail");if(!panel)return;if(!snapshot){panel.classList.add("hidden");panel.innerHTML="";return}const groups=[["本線","MAIN"],["押さえ","COVER"],["買える高配当","BUYABLE_HIGH"]];const betDetail=groups.map(([label,key])=>{const bets=(snapshot.betSelections||[]).filter(b=>b.category===key);if(!bets.length)return"";return `<div class="detailGroup"><h3>${label}</h3>${bets.map(b=>`<div class="detailBet"><strong>${b.order.join("-")}</strong>${b.branchLabel?`<p>${esc(b.branchLabel)}${b.reason?` / ${esc(b.reason)}`:""}</p>`:""}${b.evidenceSummary?`<p class="muted">${esc(b.evidenceSummary)}</p>`:""}</div>`).join("")}</div>`}).join("");const abilities=(snapshot.abilitiesUsed||[]).map(a=>`<div class="abilityRow"><strong>${a.number}番</strong><span>近況 ${fmtAbility(a.recentForm)} / 主導権 ${fmtAbility(a.startPower)} / まくり ${fmtAbility(a.sprintPower)} / 差し ${fmtAbility(a.finishPower)} / 追走 ${fmtAbility(a.trackingSkill)}</span></div>`).join("");const audit=snapshot.predictionOutput?.audit||{};const rejectLabels={FLAT_DISTRIBUTION:"分布が平坦",NO_DOMINANT_BRANCH:"支配展開なし",PROBABILITY_SUPPORT:"確率支持不足",BRANCH_OR_POSITION_SUPPORT:"枝適合/着順支持不足",CLASS_RULE:"分類条件不成立",UNKNOWN:"その他"};const rejectRows=Object.entries(audit.rejectCodeCounts||{}).sort((a,b)=>b[1]-a[1]).map(([code,count])=>`<div class="abilityRow"><strong>${esc(rejectLabels[code]||code)}</strong><span>${count}件</span></div>`).join("");const auditDetail=Number.isFinite(audit.generatedTerminalCount)?`<details class="predictionAccordion" open><summary>購入採用監査を見る</summary><div class="accordionBody"><div class="abilityList"><div class="abilityRow"><strong>生成終端</strong><span>${audit.generatedTerminalCount}件</span></div><div class="abilityRow"><strong>確率評価済み</strong><span>${audit.probabilityEvaluatedTerminalCount??audit.terminalCount??0}件</span></div><div class="abilityRow"><strong>購入採用</strong><span>${audit.adoptedTerminalCount??audit.finalBetCount??0}件</span></div><div class="abilityRow"><strong>購入不採用</strong><span>${audit.rejectedTerminalCount??0}件</span></div></div>${rejectRows?`<h3>不採用理由</h3><div class="abilityList">${rejectRows}</div>`:""}${renderBranchSelectionAudit(audit)}${renderAdoptedTerminalAudit(audit)}<p class="muted">固定点数・枝内順位上限・確率比の一律足切りは使用していません。枝数そのものは購入支持に使わず、枝強度×枝内適合の加重支持で監査します。</p></div></details>`:"";panel.innerHTML=`<div class="sectionHead"><div><small>保存済み予想の根拠</small><h2>予想詳細</h2></div><span class="pill">詳細</span></div>${auditDetail}<details class="predictionAccordion"><summary>展開・買い目の根拠を見る</summary><div class="accordionBody">${betDetail||'<p class="muted">購入候補はありません。</p>'}</div></details>${abilities?`<details class="predictionAccordion"><summary>選手別能力を見る</summary><div class="abilityList accordionBody">${abilities}</div></details>`:""}${renderStartPowerInputAudit(snapshot)}`;panel.classList.remove("hidden")}
+function renderStartPowerInputAudit(snapshot){const rows=(snapshot.abilitiesUsed||[]).filter(a=>a.startPowerEvidence).map(a=>{const e=a.startPowerEvidence||{};const rawB=e.rawBackCount,rawH=e.rawHomeCount,starts=e.officialTotalStarts;const bRate=e.bFrequency,hRate=e.hFrequency,sb=e.shrunkBFrequency,sh=e.shrunkHFrequency;const confidence=({low:"低",medium:"中",high:"高"})[e.confidence]||e.confidence||"-";const status=e.missingInputs?.length?`欠損 ${e.missingInputs.join("/")}`:`信頼度 ${confidence}`;return `<div class="detailBet"><strong>${a.number}番　主導権 ${fmtAbility(a.startPower)}</strong><p>B ${fmtAuditNumber(rawB)} / H ${fmtAuditNumber(rawH)} / 出走 ${fmtAuditNumber(starts)}　${esc(status)}</p><p class="muted">B率 ${fmtAuditRate(bRate)} / H率 ${fmtAuditRate(hRate)} → 縮小後B ${fmtAuditRate(sb)} / 縮小後H ${fmtAuditRate(sh)} / latent ${fmtAbility(e.latentScore)}</p><p class="muted">カテゴリ ${esc(e.raceCategory||"-")} / prior ${fmtAuditNumber(e.priorStrength)} / startsQuality ${fmtRatio(e.startsQuality)}</p></div>`}).join("");return rows?`<details class="predictionAccordion"><summary>主導権の入力監査を見る</summary><div class="accordionBody"><p class="muted">数値を補正する画面ではなく、B/H入力から最終startPowerまでを確認する監査表示です。</p><div class="detailGroup">${rows}</div></div></details>`:""}
+function fmtAuditNumber(v){const n=Number(v);return Number.isFinite(n)?String(Math.round(n*1000)/1000):"-"}
+function fmtAuditRate(v){const n=Number(v);return Number.isFinite(n)?`${(n*100).toFixed(1)}%`:"-"}
 function fmtAbility(v){return Number.isFinite(Number(v))?Number(v).toFixed(2):"-"}
 function noBetReasonText(code){return({NO_TERMINALS:"展開候補を生成できませんでした。",FLAT_DISTRIBUTION_NO_SUPPORTED_CANDIDATE:"確率分布が平坦で、独立した展開根拠を持つ購入候補がありません。",BUDGET_BELOW_MINIMUM:"予算が最低購入単位を下回っています。",QUALITY_GATE:"データ品質基準を満たさないため購入を見送ります。",LINE_DATA_UNAVAILABLE:"公式ラインを確認できないため、通常の競輪予想としては購入判定を保留します。"})[code]||"購入価値を確認できないため見送ります。"}
 async function checkResult(){if(state.busy)return;state.busy=true;setLoading("公式結果を確認中","レースIDを固定して確定着順と払戻を取得しています。");try{const r=state.snapshot.targetRace,q=new URLSearchParams({date:r.date,venueCode:r.venueCode,venueName:r.venueName,raceNo:String(r.raceNo)}),p=await jsonFetch(`/.netlify/functions/keirin-result?${q}`);if(raceKey(p.race)!==raceKey(r))throw new Error("公式結果のレースが保存済み予想と一致しません");if(isResultPending(p.result)){renderPrediction(state.snapshot);renderPendingResult();return}state.snapshot=attachResult(localStorage,state.snapshot.predictionSnapshotId,p.result);renderPrediction(state.snapshot)}catch(e){fail("公式結果の取得・保存に失敗",e,checkResult)}finally{state.busy=false}}
diff -ruN /mnt/data/v29orig/public/prediction-store.mjs /mnt/data/v29repo/public/prediction-store.mjs
--- /mnt/data/v29orig/public/prediction-store.mjs	2026-08-09 02:14:57.000000000 +0000
+++ /mnt/data/v29repo/public/prediction-store.mjs	2026-08-09 03:25:54.223420735 +0000
@@ -4,7 +4,7 @@
 export function createSnapshot(payload,now=new Date()){
   const race=payload.race||{},prediction=payload.prediction||{};
   const createdAt=now.toISOString(),key=raceKey(race);
-  return {predictionSnapshotId:`${key}:${createdAt}`,createdAt,targetRace:{date:normalizeDate(race.date),venueCode:String(race.venueCode||""),venueName:race.venue||race.venueName||"",raceNo:Number(race.raceNo),scheduledStart:race.startTime||race.deadline||"",deadline:race.deadline||race.startTime||""},participants:(race.participants||[]).map(p=>({number:Number(p.number),name:p.name||"",registration:p.registration||"",sourceType:p.sourceType||null,sourcePath:p.sourcePath||null})),predictionVersion:prediction.engineVersion||"STABLE",abilitiesUsed:(prediction.scored||[]).map(p=>({number:p.number,recentForm:p.recentForm??null,startPower:p.startPower??null,sprintPower:p.sprintPower??null,finishPower:p.finishPower??null,trackingSkill:p.trackingSkill??null,roleScores:p.roleScores||null,scoreTrace:p.scoreTrace||null})),predictionOutput:{recommendationLabel:prediction.recommendationLabel||"",audit:prediction.audit||null,noBet:Boolean(prediction.noBet),noBetReason:prediction.noBetReason||null},branches:prediction.branches||[],betSelections:(prediction.purchasePlan||[]).map(b=>({order:b.order.map(Number),category:b.betClass,stake:b.stake??null,odds:b.odds??null,recommendation:b.purchaseStatus||"購入採用",branchLabel:b.dominantBranchLabel||null,reason:b.purchaseReason||null,probability:b.probability??null,decisionRatios:b.decisionRatios||null,evidenceSummary:b.evidenceSummary||null,positionEvidence:b.positionEvidence||null})),category:prediction.recommendationLabel||"",recommendation:prediction.noBet?"見送り":prediction.recommendationLabel||"",noBet:Boolean(prediction.noBet),noBetReason:prediction.noBetReason||null,oddsSnapshot:payload.odds||null,result:null};
+  return {predictionSnapshotId:`${key}:${createdAt}`,createdAt,targetRace:{date:normalizeDate(race.date),venueCode:String(race.venueCode||""),venueName:race.venue||race.venueName||"",raceNo:Number(race.raceNo),scheduledStart:race.startTime||race.deadline||"",deadline:race.deadline||race.startTime||""},participants:(race.participants||[]).map(p=>({number:Number(p.number),name:p.name||"",registration:p.registration||"",sourceType:p.sourceType||null,sourcePath:p.sourcePath||null})),predictionVersion:prediction.engineVersion||"STABLE",abilitiesUsed:(prediction.scored||[]).map(p=>({number:p.number,recentForm:p.recentForm??null,startPower:p.startPower??null,startPowerEvidence:p.startPowerEvidence||null,sprintPower:p.sprintPower??null,finishPower:p.finishPower??null,trackingSkill:p.trackingSkill??null,roleScores:p.roleScores||null,scoreTrace:p.scoreTrace||null})),predictionOutput:{recommendationLabel:prediction.recommendationLabel||"",audit:prediction.audit||null,noBet:Boolean(prediction.noBet),noBetReason:prediction.noBetReason||null},branches:prediction.branches||[],betSelections:(prediction.purchasePlan||[]).map(b=>({order:b.order.map(Number),category:b.betClass,stake:b.stake??null,odds:b.odds??null,recommendation:b.purchaseStatus||"購入採用",branchLabel:b.dominantBranchLabel||null,reason:b.purchaseReason||null,probability:b.probability??null,decisionRatios:b.decisionRatios||null,evidenceSummary:b.evidenceSummary||null,positionEvidence:b.positionEvidence||null})),category:prediction.recommendationLabel||"",recommendation:prediction.noBet?"見送り":prediction.recommendationLabel||"",noBet:Boolean(prediction.noBet),noBetReason:prediction.noBetReason||null,oddsSnapshot:payload.odds||null,result:null};
 }
 export function saveSnapshot(storage,snapshot){const all=loadSnapshots(storage);const duplicate=all.find(x=>raceKey(x.targetRace)===raceKey(snapshot.targetRace)&&x.predictionVersion===snapshot.predictionVersion&&JSON.stringify(x.betSelections)===JSON.stringify(snapshot.betSelections)&&!x.result);if(duplicate)return duplicate;all.unshift(snapshot);storage.setItem(STORAGE_KEY,JSON.stringify(all.slice(0,100)));return snapshot}
 export function loadSnapshots(storage){try{const value=JSON.parse(storage.getItem(STORAGE_KEY)||"[]");return Array.isArray(value)?value:[]}catch{return[]}}
diff -ruN /mnt/data/v29orig/tests/keirin-start-power-input-audit.mjs /mnt/data/v29repo/tests/keirin-start-power-input-audit.mjs
--- /mnt/data/v29orig/tests/keirin-start-power-input-audit.mjs	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/v29repo/tests/keirin-start-power-input-audit.mjs	2026-08-09 03:25:54.941493050 +0000
@@ -0,0 +1,18 @@
+import assert from "node:assert/strict";
+import { createSnapshot } from "../public/prediction-store.mjs";
+
+const evidence={
+  value:9.3, confidence:"high", officialTotalStarts:28,
+  rawBackCount:8, rawHomeCount:8,
+  bFrequency:0.286, hFrequency:0.286,
+  shrunkBFrequency:0.211, shrunkHFrequency:0.21,
+  latentScore:9.3, startsQuality:0.651,
+  raceCategory:"standard", priorStrength:15, missingInputs:[]
+};
+const snapshot=createSnapshot({
+  race:{date:"20260809",venueCode:"28",venue:"立川",raceNo:3,participants:[{number:1,name:"A"}]},
+  prediction:{engineVersion:"KEIRIN-0.5.8-start-power-input-audit",scored:[{number:1,startPower:9.3,startPowerEvidence:evidence}],purchasePlan:[]}
+},new Date("2026-08-09T03:00:00Z"));
+assert.deepEqual(snapshot.abilitiesUsed[0].startPowerEvidence,evidence);
+assert.equal(snapshot.abilitiesUsed[0].startPower,9.3);
+console.log("keirin start-power input audit PASS");
